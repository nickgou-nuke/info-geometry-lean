# ShapeHash Cluster Proposal

**Hash:** `7373222792890993051`
**Count:** 768
**Type:** CROSS_DOMAIN_ISOMORPHISM
**Canonical Location:** `InfoGeometry.Convex.HessianGeometry:99` (`theorem`: `anon_7373222792890993051_99`)
**Source File:** `/home/goutev/repos/info-geometry-lean/lean/InfoGeometry/Convex/HessianGeometry.lean`

## Description
Cluster of 768 nodes across multiple domains sharing identical AST shape. Canonical at InfoGeometry.Convex.HessianGeometry:99.

## Canonical Source (Lean)
```lean4
  84 |   classical
  85 |   letI : BregmanDivergence ℝ ℝ := bregmanDiv H
  86 |   exact softmaxAttention (core := bregmanTriadicCore route) (I := I) (hI := hI) (keys := keys)
  87 | 
  88 | end HessianGeometry1D
  89 | 
  90 | /-- Multivariate Hessian geometry package on an inner product space `E`.
  91 | `grad` is the primal gradient and `metricOp` is recovered as `fderiv grad`.
  92 | The non-negativity witness for the induced Bregman divergence is carried
  93 | constructively as part of the datum.
  94 | -/
  95 | structure HessianGeometry (E : Type*) [NormedAddCommGroup E] [InnerProductSpace ℝ E] [CompleteSpace E] where
  96 |   potential : E → ℝ
  97 |   grad : E → E
  98 |   has_gradient : ∀ x, HasFDerivAt potential (InnerProductSpace.toDual ℝ E (grad x)) x
  99 |   divergence_form_nonneg :
 100 |     ∀ x y, 0 ≤ potential x - potential y - inner ℝ (grad y) (x - y)
 101 | 
 102 | namespace HessianGeometry
 103 | 
 104 | variable {E : Type*} [NormedAddCommGroup E] [InnerProductSpace ℝ E] [CompleteSpace E]
 105 | variable (H : HessianGeometry E)
 106 | 
 107 | /-- Dual coordinate map. -/
 108 | noncomputable def dualMap (x : E) : E := H.grad x
 109 | 
 110 | @[simp] theorem dualMap_def (x : E) : H.dualMap x = H.grad x := rfl
 111 | 
 112 | /-- Hessian metric operator, as totalized Fréchet derivative of `grad`. -/
 113 | noncomputable def metricOp (x : E) : E →L[ℝ] E :=
 114 |   fderiv ℝ H.grad x
```

## Cluster Samples

### Sample 1: `None:None` (`None`)
**Name:** `InfoGeometry.Convex.HessianGeometry.divergence_nonneg_True`


### Sample 2: `None:None` (`None`)
**Name:** `InfoGeometry.Algebraic.PrimonKreinRingConstraints.HyperbolicPrimonFlow.U`


### Sample 3: `None:None` (`None`)
**Name:** `InfoGeometry.Algebraic.PrimonKreinRingConstraints.HyperbolicPrimonFlow.U_add`


### Sample 4: `None:None` (`None`)
**Name:** `InfoGeometry.Algebraic.PrimonKreinRingConstraints.HyperbolicPrimonFlow.U_zero`


### Sample 5: `None:None` (`None`)
**Name:** `InfoGeometry.Algebraic.PrimonKreinRingConstraints.HyperbolicPrimonFlow.preservesKreinInner`


## Suggested Action
1. **Unify**: Extract the common lemma statement from the canonical shape.
2. **Generalize**: Parameterize over the domain-specific types (State, Entropy, Weyl, etc.).
3. **Prove Once**: Provide a single proof in a shared `Canonical` module.
4. **Specialize**: Each domain imports and instantiates the general lemma.

---
*Generated by ShapeHash-to-Lean Bridge*
*Cluster hash: `7373222792890993051` | Count: 768 | Canonical: InfoGeometry.Convex.HessianGeometry:99*
