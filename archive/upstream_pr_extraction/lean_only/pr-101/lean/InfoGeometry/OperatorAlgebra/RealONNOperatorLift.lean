import InfoGeometry.OperatorAlgebra.RealHestenesONNCliffordCPT
import InfoGeometry.OperatorAlgebra.ConnesSpatialDerivative
import InfoGeometry.OperatorAlgebra.ModularWeightTrace
import InfoGeometry.Canonical.BogoliubovTransport
import InfoGeometry.Canonical.ModularWeldBridge

/-!
# Real `O(n,n)` operator-lift boundary

The real operator-algebraic, Clifford, cocycle, and transport owners are the
imported modules.  This file intentionally introduces no second packet of
`Type*` witnesses and no reflexive theorem pretending to establish a KAN,
Pin, or Type-III result.  Such results belong in the corresponding native
owner once their hypotheses and proofs exist.
-/
