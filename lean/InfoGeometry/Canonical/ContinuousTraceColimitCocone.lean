import InfoGeometry.Canonical.FilteredStarInductiveCoconeTopCat
import InfoGeometry.Algebra.FiniteSpinAlgebra

/-!
# Trace-cocone export

The categorical cocone and its TopCat/state readout laws are owned by
`FilteredStarInductiveCoconeTopCat`.  This module no longer introduces a
matrix-specific `ColimitTraceState` evidence structure or a scalar colimit
surrogate.
-/
