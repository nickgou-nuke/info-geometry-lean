diff --git a/lean/DAG/Basic.lean b/lean/DAG/Basic.lean
@@
-  let valueEdges :=
-    match ci.value? with
+  let valueEdges :=
+    match ci.value? (allowOpaque := true) with
     | none => #[]
     | some v =>
         (collectExprConsts v).toList.toArray.map
           (fun n => (n, EdgeKind.value))

diff --git a/lean/DAG/ExactProoflessnessAudit.lean b/lean/DAG/ExactProoflessnessAudit.lean
@@
-  let hasDirectSorry : Bool :=
-    ci.type.hasSorry || (match ci.value? with
+  let hasDirectSorry : Bool :=
+    ci.type.hasSorry || (match ci.value? (allowOpaque := true) with
       | none => false
       | some v => v.hasSorry)

diff --git a/lean/DAG/Indexer.lean b/lean/DAG/Indexer.lean
@@
-  let valueFingerprint := ci.value?.map computeFingerprint
+  let valueFingerprint := (ci.value? (allowOpaque := true)).map computeFingerprint
@@
-  if let some v := ci.value? then
+  if let some v := ci.value? (allowOpaque := true) then
     let valDeps := (collectConsts v).toList
     for d in valDeps do
       if d != name then
         addEdge nameStr d.toString "value"
