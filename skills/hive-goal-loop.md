# 🐝 Hive Goal Loop SOP (Recurrent Cultivation)

## Purpose
This skill defines the **Recurrent Cultivation Loop** that drives the InfoGeometry proving swarm. It ensures that all objectives are cultivated through Jungian expansion and Paulian purification before attempting formal closure.

## The Goal Loop (Authoritative SOP)

1.  **Standing Goal**: Set a persistent objective via `/goal <text>`.
2.  **Cultivation State**: The goal is tracked as a `lineage` in ArangoDB, persisting across sessions.
3.  **The Judge (Paulian Pass)**: After every turn, the state is evaluated against the **UTMOST MANDATE**.
    *   **Input**: Goal + Response + Build Evidence + Graph Invariants.
    *   **Judge**: Hermes[CodexCLI] (Low temperature, strict condensation).
4.  **Recurrence Logic**:
    *   **Continue (Excitation)**: If the signal is weak, route to Jung Bees for further symbolic expansion.
    *   **Continue (Purification)**: If the signal is incoherent, route to Pauli Bees for differentiation.
    *   **Legalize**: If an invariant is stable, route to Translation Bees for packet creation.
    *   **Probe**: If a packet is legal, route to Proof Bees for kernel contact.
5.  **Completion (Done)**: A goal is `done` ONLY when certificate/witness fields have been structurally replaced with theorem-backed native derivations verified by the Lean kernel.
6.  **Residue**: If a branch fails, it is returned as `residue` (e.g., `proof_obstruction`) and preserved in graph memory for future pressure.

## UTMOST MANDATE: The Logos Gate

The Judge must enforce the following absolute rules:
-   **Anti-Evasion**: textual progress is not structural progress.
-   **Debt Tracking**: `(Native Closure Mandated: Closure Debt)` labels must persist until a kernel-verified proof term is generated.
-   **Explicit Roots**: Every theorem must be rooted in Mathlib or repo-native owner modules.

## Controls
-   `/goal status`: Shows the current position in the Recurrent Loop (Jung/Pauli/Legal/Probe).
-   `/goal residue`: Lists the failure residues currently acting as pressure on the goal.
-   `/goal clear/pause/resume`: Manual control of the meta-shell loop.

## Integration
This loop is the thermodynamic "heat engine" of the hive: Jungian heat (expansion) followed by Paulian cooling (condensation) until the formal crystal of the proof survives the Lean Logos gate.
