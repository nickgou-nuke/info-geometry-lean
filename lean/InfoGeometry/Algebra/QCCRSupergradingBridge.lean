import InfoGeometry.OperatorAlgebra.QCCRCore
import InfoGeometry.OperatorAlgebra.QCCRSupergrading

/-!
# Compatibility import for the q-CCR supergrading bridge

The q-CCR structure is owned by `OperatorAlgebra.QCCRCore`, and the
q-superbracket and boundary lemmas are owned by
`OperatorAlgebra.QCCRSupergrading`.  This module retains the legacy import
path without defining a duplicate operator structure or packet theorem.
-/
