import InfoGeometry.Canonical.ConnesSpectralTripleBridge

/-!
# Connes spectral metric export

The native noncommutative owner is `ConnesSpectralTripleBridge`.  It provides
the spectral-triple data, Lipschitz condition, and state-distance laws.  This
module intentionally contains no scalar `SpectralTripleData` surrogate.
-/
