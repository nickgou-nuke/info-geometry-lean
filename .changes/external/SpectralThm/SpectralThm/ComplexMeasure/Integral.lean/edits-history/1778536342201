{"file":"/home/goutev/repos/info-geometry-lean/external/SpectralThm/SpectralThm/ComplexMeasure/Integral.lean","time":1778536342201,"baseTime":1778536341260,"changes":[{"range":{"start":{"line":98,"character":0},"end":{"line":183,"character":0}},"text":"    (μ + ν).integral f = μ.integral f + ν.integral f := by\n  let λ := μ.variation + ν.variation\n  have : IsFiniteMeasure λ := inferInstance\n  have hμλ : μ ≪ᵥ λ.toENNRealVectorMeasure := by\n    apply VectorMeasure.AbsolutelyContinuous.mk\n    intro s hmeas hs\n    rw [Measure.toENNRealVectorMeasure_apply_measurable hmeas] at hs\n    have : ‖μ s‖ₑ ≤ μ.variation s := by\n      rw [VectorMeasure.variation, VectorMeasure.ennrealToMeasure_apply hmeas]\n      exact MeasureTheory.preVariation.sum_le (‖μ ·‖ₑ) hmeas (Finpartition.top hmeas)\n    have : μ s = 0 := by\n      have : ‖μ s‖ₑ = 0 := by \n        apply le_antisymm _ (by positivity)\n        calc ‖μ s‖ₑ ≤ μ.variation s := this\n          _ ≤ λ s := Measure.le_iff.mp (Measure.le_add_right (le_refl _)) s\n          _ = 0 := hs\n      exact enorm_eq_zero.mp this\n    exact this\n  have hνλ : ν ≪ᵥ λ.toENNRealVectorMeasure := by\n    apply VectorMeasure.AbsolutelyContinuous.mk\n    intro s hmeas hs\n    rw [Measure.toENNRealVectorMeasure_apply_measurable hmeas] at hs\n    have : ‖ν s‖ₑ ≤ ν.variation s := by\n      rw [VectorMeasure.variation, VectorMeasure.ennrealToMeasure_apply hmeas]\n      exact MeasureTheory.preVariation.sum_le (‖ν ·‖ₑ) hmeas (Finpartition.top hmeas)\n    have : ν s = 0 := by\n      have : ‖ν s‖ₑ = 0 := by \n        apply le_antisymm _ (by positivity)\n        calc ‖ν s‖ₑ ≤ ν.variation s := this\n          _ ≤ λ s := Measure.le_iff.mp (Measure.le_add_left (le_refl _)) s\n          _ = 0 := hs\n      exact enorm_eq_zero.mp this\n    exact this\n  obtain ⟨hμ, hμ_int, hμ_eq⟩ := exists_l1_eq_withDensity_variation_λ μ λ hμλ\n  obtain ⟨hν, hν_int, hν_eq⟩ := exists_l1_eq_withDensity_variation_λ ν λ hνλ\n  rw [hμ_eq, hν_eq, ← Measure.withDensityᵥ_add hμ_int hν_int]\n  rw [integral_withDensityᵥ_eq λ (hμ + hν) (hμ_int.add hν_int) f]\n  rw [integral_withDensityᵥ_eq λ hμ hμ_int f]\n  rw [integral_withDensityᵥ_eq λ hν hν_int f]\n  simp only [Pi.add_apply, smul_add]\n  rw [MeasureTheory.integral_add]\n  · exact (hμ_int.smul f).integrableOn -- assuming f is bounded\n  · exact (hν_int.smul f).integrableOn\n\nlemma integral_neg (μ  : ComplexMeasure α) (f : α → ℂ) :\n    (-μ).integral f = -μ.integral f := by\n  simp [integral, MeasureTheory.integral_neg]\n\nlemma integral_sub (μ₁ μ₂  : ComplexMeasure α) (f : α → ℂ) :\n    (μ₁ - μ₂).integral f = μ₁.integral f - μ₂.integral f := by\n  calc\n    _ = (μ₁ + (-μ₂)).integral f := by rfl\n    _ = μ₁.integral f + (-μ₂).integral f := by exact integral_add μ₁ (-μ₂) _\n    _ = _ := by rw [integral_neg, sub_eq_add_neg]\n\n/-- The integral is linear with respect to the measure: `∫ f d(c • μ) = c • ∫ f dμ` -/\nlemma integral_smul_measure (c : ℂ) (μ : ComplexMeasure α) (f : α → ℂ) :\n    (c • μ).integral f = c • μ.integral f := by\n  -- Proof outline: similar to `integral_add`, use RN density with respect to a dominating measure.\n  sorry\n\n/-- The integral is linear with respect to the function: `∫ (c • f) dμ = c • ∫ f dμ` -/\nlemma integral_const_smul (c : ℂ) (μ : ComplexMeasure α) (f : α → ℂ) :\n    μ.integral (fun a => c • f a) = c • μ.integral f := by\n  dsimp [integral]\n  simp_rw [smul_comm]\n  rw [MeasureTheory.integral_smul]\n\n/-- The integral is linear with respect to the function: `∫ (f + g) dμ = ∫ f dμ + ∫ g dμ` -/\nlemma integral_add_function (μ : ComplexMeasure α) (f g : α → ℂ) :\n    μ.integral (fun a => f a + g a) = μ.integral f + μ.integral g := by\n  dsimp [integral]\n  simp_rw [smul_add]\n  rw [MeasureTheory.integral_add]\n  · exact (Integrable.smul μ.ang (sorry : Integrable f μ.var)).integrableOn\n  · exact (Integrable.smul μ.ang (sorry : Integrable g μ.var)).integrableOn\n\n/-- The integral of `f` against `μ` is bounded by `variation μ * ‖f‖_∞`. -/\nlemma integral_bound_of_variation_le (μ : ComplexMeasure α) (f : α → ℂ) (C : ℝ)\n    (hf : ∀ x, ‖f x‖ ≤ C) : ‖μ.integral f‖ ≤ (μ.variation Set.univ).toReal * C := by\n","rangeOffset":3267,"rangeLength":4009}]}