# maxentmom.tex

2

## Abstract

A moment body is a linear projection of the spectraplex, the convex set of trace-one positive semidefinite matrices. Determining whether a given point lies within a given moment body is a problem with numerous applications in quantum state estimation or polynomial optimization. This moment body membership oracle can be addressed with semidefinite programming, for which several off-the-shelf interior-point solvers are available. In this paper, inspired by techniques from quantum information theory, we argue analytically and geometrically that a much more efficient approach consists of minimizing globally a smooth strictly convex log-partition function, dual to a maximum entropy problem. We analyze the curvature properties of this function and we describe a neat geometric pre-conditioning algorithm. A detailed complexity analysis reveals a cubic dependence on the matrix size, similar to a few eigenstructure computations. Basic numerical experiments illustrate that dense (i.e. non-sparse) projections of size 1000 of a dense semidefinite matrix of size 1000-by-1000 can be routinely handled in a few seconds on a standard laptop, thereby moving the main bottleneck in large‐scale semidefinite programming almost entirely to efficient gradient storage and manipulation.

## Introduction

Semidefinite programming is a versatile framework for convex optimization. It consists of optimizing (typically linear functions) over spectrahedra (linear sections of the semidefinite cone, described by linear matrix inequalities) or spectrahedral shadows (linear projections of spectrahedra). These sets capture a large class of convex semialgebraic sets [cite:bn01]. Polynomial optimization relies heavily on semidefinite optimization, and the moment-SOS hierarchy constructs a nested family of spectrahedral shadows of increasing size that provide increasingly tight approximations of convex hulls of semialgebraic sets, see e.g. [cite:hkl20,n23,t24] and references therein.

Semidefinite optimization problems can be solved with interior-point algorithms [cite:nn94,bn01]. However, as second-order methods, these algorithms do not scale well at the age of data science. Most of the computational burden is concentrated on computing and storing the Hessian matrix of second-order derivatives of a logarithmic barrier function. First-order algorithms scale better, since they use only gradient information, but they are also more sensitive to problem scaling and conditioning. Conditioning of semidefinite optimization problems is understood theoretically [cite:renegar95], but evaluating the conditioning of a given problem is as expensive as solving the original problem. From that point of view, the versatily and generality of semidefinite programming can also be seen as a weakness: currently, there is no simple recipe that can be systematically used to cure all numerical issues, see e.g. [cite:pataki19] for a survey of recent attempts. There are at least three geometric pathologies that can occur in semidefinite programming: (i) a linear image of an unbounded spectrahedron need not be closed; (ii) a spectrahedron or its shadow can lack interior points; (iii) the linear map defining a spectrahedral shadow can be ill-conditioned (i.e. with singular values largely differing in magnitude). In this paper, we propose to focus on pathology (iii), namely ill-conditioning of the linear map, and our strategy is as follows. First, we restrict our attention to semidefinite feasibility problems whose spectrahedral shadows are full-dimensional and bounded. This eliminates the pathologies (i) and (ii). Second, we focus on analytic, quantitative aspects of a standard first-order optimization algorithm, in which issue (iii) appears explicitly through curvature parameters. This allows us to design a simple and cheap pre-conditioning algorithm.

Our focus is on the moment body membership oracle problem: finding a point in a linear projection of the spectraplex, defined as the compact convex set of trace-one positive semidefinite matrices, a non-polyhedral generalization of the simplex. Determining whether a given point lies within a given moment body is a problem with numerous applications in polynomial optimization or quantum information theory. This includes for example the problem of decomposing a given multivariate polynomial as a sum of squares (SOS) of other polynomials, see e.g. [cite:n23] and references therein.
In order to address this problem with a first-order algorithm, we use an approach inspired from quantum information theory [cite:jv23,h24], namely the global minimization of a smooth and strictly convex log-partition function dual to a maximum entropy problem. Quantum state estimation aims to recover a density matrix (i.e. an element of the spectraplex, a trace-one positive semdefinite matrix) consistent with observed measurement statistics (i.e. the linear projection of the spectraplex) [cite:bz17] - and this is exactly our moment body membership oracle problem. A particularly effective method for solving this problem consists of selecting, among all compatible density matrices, the one maximizing entropy. The dual of this problem leads to the minimization of a convex, smooth function called the log-partition function. We analyze its curvature properties, and based on geometric quantities appearing during the analysis, we describe a neat and simple geometric pre-conditioning algorithm. A detailed complexity analysis reveals a cubic dependence on the matrix size, similar to a few eigenstructure computations.

Basic numerical experiments illustrate that a rudimentary Matlab prototype can be competitive with SDPNAL+ [cite:sdpnal15,sdpnal20] a state-of-the-art solver for large-scale semidefinite programming. On a standard laptop we can solve in a few seconds the moment body membership for a dense (i.e. non-sparse) linear projection of size 1000 of a dense semidefinite matrix of size 1000-by-1000, at expected accuracy $10^{-8}$. Note however that for these sizes, just storing the problem data requires almost 8 gigabytes. Practically speaking, this implies that, for this problem class, the bottleneck of large-scale semidefinite solvers is pushed further and almost exclusively to the efficient storage and manipulation of gradient information.

### Outline

The paper is organized as follows. Section 2 defines the moment body and presents a few examples to illustrate its geometry in low dimension. In Section 3 we show how testing membership in a moment body of size $m$ defined by a spectraplex of size $n$-by-$n$ can be formulated as the unconstrained minimization in $\R^m$ of a smooth, strictly convex log‐partition function, and we prove that this dual problem is equivalent (via strong duality) to a primal maximum‐entropy formulation. Section 4 is devoted to a first geometric analysis of the dual objective. We derive explicit upper and lower bounds on its Hessian in terms of the Gram matrix of the linear map defining the moment body, showing that the dual is
globally $\lambda$-smooth and $\alpha$-strongly convex on sublevel sets, with $\lambda$ and $\alpha$ depending only on the spectrum of the Gram matrix. In Section 5 we present a simple preconditioning algorithm: by centering and orthonormalizing the linear map, one can force the dual to become $\frac{1}{2}$-smooth and $\frac{1}{n^3}$-strongly convex. In Section 6 we exploit these curvature estimates to bound the size of the unique minimizer in terms of the input data. Section 7 gives a detailed iteration‐complexity analysis of L-BFGS when applied to the preconditioned dual.
Section 8 discusses how the same dual framework detects weakly feasible points (boundary membership) and certifies strict infeasibility. Section 9 briefly explains how our analysis extends to block‐separable (direct‐sum) moment‐body problems, in which the primal density matrix splits into several independent blocks. Finally, Section 10 presents numerical experiments on random dense instances: we compare our Matlab prototype
againt off-the-shelf semidefinite solvers and demonstrate that dense problems of size $n=m=1000$ can be solved in a few seconds on a standard laptop.

### Notations

$\S^n$ is the space of real valued symmetric matrices of size $n$, $$\S^n_+ := \{X \in \S^n, X \succeq 0\}$$ is the convex closed cone of positive semidefinite elements of $\S^n$, called the semidefinite cone. Its interior $$int\:\S^n_+ := \{X \in \S^n, X \succ 0\}$$ is the convex open cone of positive definite elements of $\S^n$, and $$\S^n_1:=\{X \in \S^n, X \succeq 0, \trace X = 1\}$$ is called the spectraplex, a generalization to non-diagonal matrices of the polyhedral simplex. It is a spectrahedron, an affine slice of the semidefinite cone, see e.g. [cite:n23].

Given a matrix $X$, $\log X$ denotes its logarithm, $\exp X$ its exponential, and $$\exp_1 X := \frac{\exp X}{\trace \exp X}$$ is the normalized or trace-one exponential.

## The moment body

Let $A_i \in \S^n$, $i=1,\ldots,m$ be given matrices.
Define the linear map $\mathcal A : \S^n \to \R^m, X \mapsto \trace(A_i X)_{i=1,\ldots,m}$ and its adjoint $\mathcal A^T : \R^m \to \S^n, \by \mapsto A(\by):=\sum_{i=1}^m y_i A_i$.
The moment body of $\mathcal A$ is the set

$$

\boxed{
{\mathscr M}:= \left\{ \mathcal A(X) : X \succeq 0, \trace X = 1\right\} \subset \R^m}

$$

or equivalently $\mathscr M:=\mathcal A(\S^n_1)$. In words, a moment body is the linear image of a spectraplex.
As a linear projection of a convex and compact set, set $\mathscr M$ is also convex and compact. The terminology moment body is motivated as follows.
Let $\mathscr X$ be a topological space, and let $\phi : \mathscr X \to \mathscr U$ be a map,
For example, $\phi(\bx)$ can be the result of a measurement for $\bx \in \mathscr X$ in some given set of Euclidean space, with $\phi$ a basis for the vector space of polynomials of $\bx$ up to some degree.
We can identify each matrix $A_k$ with the Gram matrix of a function $a_k : \mathscr X \to \R, \phi(\bx) \mapsto \phi^T(\bx) A_k \phi(\bx)$ and then write $\trace(A_kX) = \int_{\mathscr X} a_k(\bx) d\mu(\bx)$ where $X = \int_{\mathscr X} \phi(\bx) \phi(\bx)^T d\mu(\bx)$ is the moment matrix of $\mu$, an element of $\mathscr P(\mathscr X)$, the set of probability measures on
$\mathscr X$. Equivalently, if we define $\nu$ as the image measure of $\mu$ through $\phi$, $X = \int_{\mathscr U} \bu\bu^T d\nu(\bu)$ is the covariance matrix of $\nu \in \mathscr P(\mathscr U)$.
Both measures satisfy $\int_{\mathscr X} d\mu(\bx) = \int_{\mathscr U} d\nu(\bu) = \int_{\mathscr U} \bu^T \bu \,d\mu(\bu) = \trace X =1$.
The moment body is therefore the set of all moments of such probability measures, i.e.

$$

\mathscr M = \left\{ \int_{\mathscr X} \ba(\bx) d\mu(\bx) \::\: \mu \in \mathscr P(\mathscr X)\right\}.

$$

is also called the set of mixed quantum states in quantum information theory [cite:bz17]. Its elements are known as density operators or density matrices. Its extreme points are rank-one matrices generated by Alternatively, we can also interpret the moment body as a generalized numerical range -- see e.g. [cite:psw21,nv24] and references therein --
defined as the convex hull of the image of the unit sphere through the linear map $\mathcal A$, i.e.

$$

\mathscr M = \text{conv} \mathcal A(\mathscr U) = \text{conv}\{[\bu^T A_i \bu]_{i=1,\ldots,m}, \:\bu \in \mathscr U\}.

$$

Finally, as a linear projection of a spectrahedron, the moment body is a spectrahedral shadow, see e.g. [cite:n23].

Throughout the paper, we make the following natural assumption on the linear map.

#injective

## Example [label:ex:momentbody2d]

Let $n=3$, $m=2$ and

$$

A_1 =
\frac{1}{2}\left(\begin{array}{rrr}
1 & 1 & 0 \\ 1 & 0 & 0 \\ 0 & 0 & -1
\end{array}\right), \quad
A_2 =
\frac{1}{2}\left(\begin{array}{rrr}
-1 & 1 & 0 \\ 1 & 0 & 0 \\ 0 & 0 & 1
\end{array}\right).

$$

As explained e.g. in [cite:h10], the moment body of $\mathcal A$ is the convex hull of the algebraic curve dual to the curve $\{ \by \in \R^2 : p(\by)=\det(I_3+A_1y_1+A_2y_2)\}$, i.e. the envelope of all tangent lines. The determinant factors into $p(\by)=\frac{1}{8}(4+2y_1-2y_2-y^2_1-2y_1y_2-y^2_2)(2-y_1+y_2)$, so the dual curve is the union of the ellipse $\{\bx \in \R^2 : 5x^2_1-6x_1x_2+5x^2_2-4x_1+4x_2=0\}$ and the point $(-\frac{1}{2},\frac{1}{2})$. Equivalently, in parametric form, the moment body of $\mathcal A$ is the convex hull of the ellipse

$$

\left\{\left(\trace\frac{1}{2}\left(\begin{array}{ccc}1 & 1 & 0 \\ 1 & 0 & 0 \\ 0 & 0 & -1 \end{array}\right)
\left(\begin{array}{c}\cos\theta \\ \sin\theta \\ 0\end{array}\right)
\left(\begin{array}{c}\cos\theta \\ \sin\theta \\ 0\end{array}\right)^T,
\trace\frac{1}{2}\left(\begin{array}{ccc}-1 & 1 & 0 \\ 1 & 0 & 0 \\ 0 & 0 & 1 \end{array}\right)\left(\begin{array}{c}\cos\theta \\ \sin\theta \\ 0 \end{array}\right)
\left(\begin{array}{c}\cos\theta \\ \sin\theta \\ 0 \end{array}\right)^T\right), \theta \in [0,2\pi]\right\}

$$

and the point

$$

\left\{\left(\trace\frac{1}{2}
\left(\begin{array}{ccc}1 & 1 & 0 \\ 1 & 0 & 0 \\ 0 & 0 & -1 \end{array}\right)
\left(\begin{array}{c}0 \\ 0 \\ 1 \end{array}\right)
\left(\begin{array}{c}0 \\ 0 \\ 1 \end{array}\right)^T,
\trace\frac{1}{2}\left(\begin{array}{ccc}-1 & 1 & 0 \\ 1 & 0 & 0 \\ 0 & 0 & 1 \end{array}\right)
\left(\begin{array}{c}0 \\ 0 \\ 1 \end{array}\right)
\left(\begin{array}{c}0 \\ 0 \\ 1 \end{array}\right)
\right) \right\}.

$$

See Figure [ref:fig:momentbody2d].

## Example [label:ex:momentbody3d]

Consider the two unit circles in orthogonal planes in~$\R^3$:

$$

C_{x_1x_2} = \{(\cos\theta,\;\sin\theta,\;0)\colon\theta\in[0,2\pi]\},
\quad
C_{x_1x_3} = \{(\cos\phi,\;0,\;\sin\phi)\colon\phi\in[0,2\pi]\}.

$$

Their convex hull can be modeled as a moment body as follows. Define the matrices

$$

J =
\begin{pmatrix}
1 & 0 \\
0 & -1
\end{pmatrix},
\:
K =
\begin{pmatrix}
0 & 1 \\
1 & 0
\end{pmatrix},
\:
A_1 = \begin{pmatrix} J & 0 \
$$
3pt] 0 & J ,
\:
A_2 = K & 0 \
$$
3pt] 0 & 0_{2} \end{pmatrix},
\:
A_3 = \begin{pmatrix} 0_{2} & 0 \
$$
3pt] 0 & K ,

$$

i. e. $A_i \in \S^4$, $i=1,2,3$.
The corresponding moment body is the convex hull of the union of the circles $C_{x_1x_2}$ and $C_{x_1x_3}$. Indeed, $\mathcal A(X_1\oplus0_{2}) = \{(\trace X_1 J), \trace (X_1 K), 0) : X_1 \in \S^2_1\} = \conv C_{x_1x_2}$, $\mathcal A(0_2\oplus X_2) =\{(\trace X_2 J), 0, \trace (X_2 K), 0) : X_2 \in \S^2_1\} = \conv C_{x_1x_2}$, and $\mathcal A(\S^4_1)$ consists of all convex combinations of these two sets, see Figure [ref:fig:momentbody3d].

## The moment body membership oracle

Given the linear map $\mathcal A$, the moment body membership oracle consists of determining whether a given vector $\bb \in \R^m$ belongs to $\mathscr M$.

Let

$$

$$

be the cumulant generating function or log-partition function.

## Lemma [label:gh]

Function $f$ is smooth and convex on $\R^m$. Its gradient is $$ f( ) = _i=1, ,m = A(X( ))- _MATH_85@@

^2 f( ) = _i=1, ,m.

$$

where $$X( ):= _1 A( ) ^n_1$$ is a so-called density matrix.

## Proof [label:duhamel]

By standard matrix‐calculus, $X\mapsto \exp X$ is smooth on $\S^n$, so
$\log\Tr\exp X$ is smooth as a composition.
Let $t(\by)=\trace\exp A(\by)$ so that $ f(\by)=\log t(\by)\;-\;\by^Tb.$

\medskip\noindent{\bf First derivatives. }
\new{
By the Duhamel formula for the derivative of a matrix exponential [cite:Wilcox1967], we have

$$
y_i
= _0^1
((1-s)A( ) )\,A_i\, (sA( ) )\,ds.
$$

Taking the trace gives

$$
y_i
= _0^1
( ((1-s)A( ))\,A_i\, (sA( )) )\,ds
= (A_i\, A( ) )
$$

by the cyclic property of the trace. Therefore

$$

y_i
= t( )\, y_i
= ( A( ))
= (A_i\,X( ))

$$

and finally

$$

y_i
= (A_i\,X( ))-b_i.

$$

}

\medskip\noindent{\bf Second derivatives. }
\new{
Let us differentiate the gradient

$$
y_i y_j
= y_j\
= (A_i\, y_j ).
$$

First develop

$$

y_j
= t( )\, y_j
\;-\; t( )^2\, y_j

$$

and use relation [ref:eq1]:

$$

y_j
= (A_j A( ))
=t( )\, (A_j X( ))

$$

to obtain

$$

y_j
= t( )\, y_j
\;-\;X( )\, (A_j X( )).

$$

We use again Duhamel's formula [ref:duhamel] to obtain

$$

y_j
= _0^1 t( ) (s\,A( ) )\,A_j\, ((1-s)\,A( ) )\,ds
\;-\;X( )\, (A_j\,X( ) ).

$$

Substituting this expression into relation [ref:eq2] we get

$$

y_i y_j
= _0^1 t( )
(A_i\, (sA( ))\,A_j\, ((1-s)A( )) )\,ds
\;-\; (A_i\,X( ))\, (A_j\,X( )).

$$

Since $X(\by)$ is symmetric and $s \in [0,1]$ it holds

$$

X( )^s= t( )^s,

X( )^1-s= t( )^1-s

$$

and we have

$$

(sA( ))\,A_j\, ((1-s)A( ))
= t( )\,X( )^s\,A_j\,X( )^1-s.

$$

Substituting this expression under the integral, we finally obtain

$$
y_i y_j
= _0^1
(A_i\,X( )^s\,A_j\,X( )^1-s )\,ds
\;-\;
(A_i\,X( ) )\, (A_j\,X( ))
$$

which is the expected expression.
Note that these expressions were already studied in quantum information theory, see e.g. \cite [Lem. VI]{w63}, the Bogoliubov-Kubo-Mori (BKM) inner product in [cite:an00] or [cite:w14].
}

\medskip\noindent{\bf Convexity. }
\new{Let us show that for any direction $\bu\in\R^m$ and any vector $\by \in \R^m$ it holds

$$

^T ^2 f( )\, \; 0.

$$

From relation [ref:eq3] it holds

$$
^T ^2 f( )\,
= _0^1
(A( )\,X( )^s\,A( )\,X( )^1-s )\,ds
\;-\;
^2.
$$

For each fixed $s\in[0,1]$, define the bilinear form

$$

Y,Z _s \;:=\; (Y\, X^s( )\,Z\,X^1-s( ) ).

$$

This form satisfies the properties of an inner product because $X(\by)$ is positive definite.
Then by the usual Cauchy–Schwarz inequality for this inner product,

$$

A( ),A( ) _s I,I _s
\; \;
I,A( ) _s^2

$$

since $\langle I,I\rangle_s>0$. But

$$

I,A( ) _s
= (I\,X^s( )\,A( )\,X^1-s( ) )
= (A( )\,X( ) ),

$$

and

$$

I,I _s
= (I\,X^s( )\,I\,X^1-s( ) )
= X( )
=1.

$$

Hence

$$
A( ),A( ) _s = (A( )\,X^s( )\,A( )\,X^1-s( ) )
\; \;
^2.
$$

Integrating over $s\in[0,1]$ gives

$$

_0^1
(A( )X^s( )A( )X^1-s( ) )\,ds
\; \;
_0^1 ^2\,ds
= ^2.

$$

Therefore

$$

^T ^2 f( )\,
\;=\;
_0^1 (A( )X^s( )A( )X^1-s( ))\,ds
\;-\; ^2
\; \;0,

$$

and so $\nabla^2 f(\by)$ is positive semidefinite.
}

## Lemma [label:coercive]

Function $f$ is coercive (i.e.\ $\lim_{\|y\|\to\infty}f(y)=+\infty$)
if and only if $\bb \in \Int \mathscr M$.

## Proof

If $\bb\notin\Int \mathscr M$, by the separating hyperplane theorem there exists a vector $\bu \in \mathscr U$ so that
$\lambda_{\max}\bigl(A(\bu)\bigr)<\bu^T \bb$. Along the ray $\by=t\,\bu$,

$$

f(t\, )
= t A( ) - t\, ^T
\; \;t\, _ ( A( )) - t\, ^T
\; \;-\,
\: \:t + ,

$$

so $f$ cannot be coercive.

Conversely,
from the inequality $\sup_{\by \in \mathscr M} \bu^T \by = \lambda_{\max}(A(\bu))$, if $\bb\in\Int \mathscr M$ then for every vector $\bu \in \mathscr U$,
$\lambda_{\max}(A(\bu))>\bb^T \bu$. Hence along the ray $\by = t\bu$,

$$

f(t\, )
\; \;
t\, _ ( A( )) - t\, ^T
\; \;+\,
\: \:t + .

$$

## Lemma [label:unique]

If $\bb \in \Int \mathscr M$, function $f$ has a unique global minimizer

$$

f^* = _ ^m f( ).

$$

## Proof

\new{Recall expression [ref:eq4] from the proof of Lemma [ref:gh], let

$$

V( , ) := _0^1
(A( )\,X^s( )\,A( )\,X^1-s( ))\,ds
-
^2

$$

and let us show that $V(\by, \bu)$ is zero if and only if $A(\bu)$ is a multiple of the identity matrix.

Assume $A(\bu) = cI$ for some scalar $c \in \mathbb{R}$. We substitute this into the expression for $V(\by, \bu)$.
The first term becomes:

$$

_0^1 ((cI)\,X^s( )(cI)X^1-s( )c )\,ds = _0^1 (c^2 X^s( ) X^1-s( ) )\,ds = _0^1 c^2 X( )\,ds = c^2.

$$

The second term becomes:

$$

^2 = ^2 = c^2.

$$

Therefore, $V(\by, \bu) = 0$.

The converse statement relies on the Cauchy-Schwarz inequality [ref:eq5] obtained from the inner product $I,\langle A(\bu)\rangle_s$ defined in the proof of Lemma [ref:gh].
This inequality shows that the integrand in the expression for $V(\by, \bu)$ is non-negative for all $s \in [0,1]$. If $V(\by, \bu) = 0$ it holds

$$

_0^1 ( (A( )X^s( ) A( ) X^1-s( )) ) ds =
^2.

$$

Since the integrand is a continuous and non-negative function of $s$, its integral can only be zero if the integrand is identically zero for all $s \in [0,1]$. Therefore:

$$

( A( )X^s( ) A( ) X^1-s( )) = ^2 s [0,1].

$$

This means the Cauchy-Schwarz inequality [ref:eq5] must hold with equality for all $s$. Equality in the Cauchy-Schwarz inequality holds if and only if the two matrices $A(\bu)$ and $I$ are linearly dependent, meaning $A(\bu)$ must be a multiple of the identity matrix.

Under Assumption 1, the matrices $A_1, \ldots, A_m$ cannot span the identity matrix, and hence $A(\bu) = cI$ implies that $c=0$, and thus $A(\bu) = \mathbf{0}$. The linear independence of the matrices then forces $\bu = \mathbf{0}$.

Therefore, $\bu^T\nabla^2 f(\by)\,\bu > 0$ for all $\bu \neq \mathbf{0}$, proving that $f$ is strictly convex. A strictly convex function has at most one minimizer. Since $f$ is also coercive when $\bb \in \Int \mathscr M$ (by Lemma [ref:coercive]), it is guaranteed to have a unique global minimizer.
}

The above results suggest that minimizing $f$ solves the moment body membership oracle. Indeed, $f$ has a unique global minimizer $\by^*$ at which the gradient of $f$ vanishes, i.e. $\mathcal A(X(\by^*))=\bb$. Therefore the inclusion $\bb\in \mathscr M$ is certified by the matrix $X(\by^*)=\exp_1 A(\by^*) \in \S^n_1$.

## Theorem [label:primaldual]

The convex unconstrained minimization problem

$$

_ ^m f( )

$$

is dual to the convex problem of maximizing the entropy in the pre-image of the moment body

$$

_X ^n_1 (X-X X) s.t. A(X) =

$$

At the optimum $(X^*,\by^*)$ it holds

$$

$$

## Proof

Introduce multipliers $\by\in\mathbb{R}^m$, $z\in\mathbb{R}$, and $Z\in \S^n_+$ for these constraints, respectively. The Lagrangian is

$$

L(X, ,z,Z)
= (X-X X)
- ^T ( A(X)- )
- z( X-1)
- (XZ).

$$

Using the matrix‐derivative identity
$\partial_X\Tr(X - X\log X)=-\log X$, setting $\nabla_X\mathcal L=0$ gives

$$

X - A( ) - z I - Z = 0.

$$

At the optimum $X^*\succ0$, so $Z^*=0$. Thus

$$

X^* - A( ^*) - z^* I = 0
\; \;
X^* = ( A( ^*)+z^* I )
= z^*\, A( ^*).

$$

The constraint $\trace X^*=1$ enforces $z^*=-\log\trace\exp A(\by^*)$ and hence

$$

X^*=
A( ^*) = _1 A( ^*)\,.

$$

From dual optimality, it holds $\mathcal A(X^*)=\bb$, i.e. $X^* \in \mathcal A^{-1}(\bb)$.`

The following result is well-known in quantum information theory, see e.g. [cite:jv23], where it is attributed to [cite:w63].

## Lemma

The map $\by \mapsto \new{\mathcal A}(\exp_1 A(\by))$ is a smooth diffeomorphism between $\R^m$ and $\Int \mathscr M$.

## Proof

Define $g(\by)=\log\trace\exp A(\by)=f(\by)+\bb^T\by$ and $\Phi(\by) = \nabla g(\by) = \mathcal A(X(\by))$ with $X(\by)=\exp_1 A(\by)$.
We claim that
$\Phi:\;\mathbb R^m\;\longrightarrow\;
\Int\mathscr M$
is a smooth diffeomorphism from the whole space onto the interior of the moment body.

- {Smoothness.} Since $f$ is smooth, $g$ is smooth, and its gradient $\Phi$ is smooth.

- {Injectivity.}
$g$ is strictly convex, so $\Phi$ is injective.

- {Local invertibility.}
$\nabla^2 g(\by)$ is positive‐definite for all $\by$, hence $\nabla \Phi (\by)=\nabla^2g(\by)$ is invertible everywhere. By the inverse‐function theorem, $\Phi$ is a diffeomorphism.

- {Image equals the interior.}
For any $\by$, $\Phi(\by)=\mathcal A(X(\by))$ where $X(\by)\succ0$, so $\Phi(\by)$ lies in the {interior} of $\mathscr M$.
Conversely, given any interior point $\bx$ of the moment body, strict convexity of $g$ and the Legendre‐transform duality imply there is a unique $\by$ solving $\nabla g(\by)=\bx$.

- {Properness i.e. surjectivity onto the interior.}
Strict convexity plus coercivity of $g$ ensure $\|\Phi(\by)\|\to\infty$ as $\|\by\|\to\infty$, forcing the range of $\Phi$ to be open, closed, and nonempty in the interior of $\mathscr M$, hence equal to it.

## Example [label:ex:2d]

For the matrices of Example [ref:ex:momentbody2d],
the graph of function $f(y)$ is represented on Figure [ref:fig:dualfunction], together with a regular grid $\mathscr Y$ (black lines underneath).
The moment body $\mathscr M$ is represented on Figure [ref:fig:momentbodygrid2d], together with the image of the grid through the gradient map $\mathcal A(\exp_1 \mathcal A^T(\mathscr Y))$.

## Geometric analysis

\new{In this section,} let us make the following
\begin{assumption}{#interior}
$\bb \in \Int \mathscr M$\new{.}
\end{assumption}
\new{This} implies that $f$ has a unique minimizer, see Lemmas [ref:gh] and [ref:coercive].
Since $f$ is smooth and convex, its minimization can be achieved with standard optimization algorithms.
The performance of these algorithms depends on the geometry of $f$, and especially its curvature.
We say that $f$ is $\alpha$-strongly convex and $\lambda$-smooth whenever

$$

0 I_m ^2f( ) I_m
\, ^m.

$$

The constant $\alpha>0$ is called the strong convexity modulus, and the constant $\lambda>0$ is called the smoothness constant (or Lipschitz constant of $\nabla f$).
The condition number

$$

=

$$

governs the convergence rates of standard first‐ and second‐order methods.
With fixed step‐size $1/\lambda$, gradient descent
$\;\by_{k+1}=\by_k-\alpha\nabla f(\by_k)$
satisfies the linear rate
$
f(\by_k)-f(\by^*)
\;\le\;(1-\frac{1}{\kappa})^k (f(\by_0)-f(\by^*)),
$
so that reaching $\varepsilon$-accuracy to the minimum $\by^*$ requires
$\,O(\kappa\log\varepsilon^{-1})$ iterations, see
e.g. [cite:BoydVandenberghe04].
Nesterov’s accelerated scheme achieves the optimal first-order complexity $O(\sqrt{\kappa}\;\log\frac1\varepsilon)$
by combining momentum with gradient steps, see
[cite:Nesterov04].
Near the optimum, Newton’s method
$\;\by_{k+1}=\by_k - [\nabla^2f(\by_k)]^{-1}\nabla f(\by_k)$
converges quadratically, but its {region of attraction} and the quality
of each step depend on $\kappa$. Ill‐conditioned Hessians can force
small steps or necessitate line‐search/globalization strategies, whose
complexity again scales with $\kappa$, see [cite:NocedalWright06].
Interior‐point methods exhibit polynomial‐time complexity bounds that
depend on the barrier Hessian’s conditioning $\kappa$
(see [cite:Wright94IPM]), and quasi‐Newton updates
(e.g.\ BFGS) achieve superlinear convergence only when $\kappa$
is moderate. Consider running L-BFGS with memory parameter $m_{\rm hist}$ and a standard Wolfe line‐search starting from $y_0$. Then the L-BFGS iterates $(\by_k)$ satisfy
$f(\by_k)-f(\by^*) \;\le\;
\rho^k\,(f(\by_0)-f(\by^*))$,
where the rate
$\rho\;=\;1 - \frac{c}{\,\kappa\,m_{\rm hist}\,}$
for some constant $c$, showing the impact of the conditioning $\kappa$. Equivalently,
$\|\by_k - \by^*\|_2 \le
C\,\rho^{\,k/2}\,\|\by_0 - \by^*\|_2$, for some constant $C$, see e.g. [cite:NocedalWright06].

To solve the moment body membership problem for moderate size problems ($n,m \approx 1000$), we propose to use L-BFGS, a standard quasi-Newton algorithm constructing an approximation of the Hessian using a limited number of evaluations of the gradient. It can be interpreted as a discretization of a variable-metric generalization of the Newton flow where the true inverse Hessian is replaced by a time-varying symmetric positive-definite matrix.

In the context of semidefinite optimization, the idea of formulating and solving with BFGS a dual smooth problem was already explored in [cite:m04] for the semidefinite least-squares problem, consisting of projecting a given symmetric matrix onto a given spectrahedral shadow. It was later on used to solve polynomial SOS problems [cite:hm11].

In this section, we derive bounds on the curvature of $f$ depending explicitly on the problem data. For this we need to
define the Gram matrix

[equation:gram]
$$
G:= _i,j=1, ,m.
$$

## Lemma: Smoothness [label:smoothness]

Let $\lambda:=\lambda_{\max}(G)$.
Function $f$ is $\new{\frac{1}{2}}\lambda$-smooth.

## Proof [label:upperbound]

To prove that $f$ is $\frac{1}{2}\lambda$-smooth, we must show that $\bu^T\nabla^2f(\by)\,\bu \le \frac{1}{2}\lambda \|\bu\|_2^2$ for any $\bu\in\R^m$ and $\by\in\R^m$.

\new{
Let $\by$ and $\bu$ be given, arbitrary. For notational ease, we will write $X := X(\by)$ and $A := A(\bu)$.
Let

$$

X
=V\, ( _i)\,V^T, V = [v_1 v_n], V^T V = I_n

$$

be the spectral decomposition of positive definite symmetric matrix $X$, so that for all $s \in [0,1]$ it holds

$$

X^s=V\, ( _i^s)\,V^T,

X^1-s=V\, ( _i^1-s)\,V^T

$$

and hence

$$

A\,X^s\,A\,X^1-s
=A\,V\, ( _i^s)\,V^T\,A\,
V\, ( _j^1-s)\,V^T.

$$

Taking the trace we get

$$

(AX^sAX^1-s)
= ( ( _i^s)\;V^T AV\; ( _j^1-s)\;V^T AV).

$$

Writing out the diagonal product, the $(i,i)$ entry of the right hand side matrix is

$$

_j=1^n _i^s\,(V^T AV)_ij\,
_j^1-s\,(V^T AV)_ji.

$$

Since $A$ is symmetric, $(V^T AV)_{ji} = (V^T AV)_{ij}
$ and hence

$$

(AX^sAX^1-s)
= _i=1^n _j=1^n
_i^s\, _j^1-s\,
(v_i^T\,A\,v_j)^2.

$$

For $s \in [0,1]$, let $$g(s) := (AX^sAX^1-s).$$
Using the above expression, we can write $$g(s)= _i,j g_ij (s( _i- _j))$$ for some non-negative coefficients $g_{ij}$.
Since each term $\exp(sa)$ is convex in $s$, $g(s)$ is convex in $s$ and hence $$ _0^1 (AX^sAX^1-s) ds = _0^1 g(s)ds (g(0),g(1)) = (A^2\,X).$$ Recalling the expression [ref:eq4] of the second directional derivative it holds

$$
^T ^2f( )\,
(A^2( )\,X( )) -
[ (A( )X( ))]^2 2 ((A )^2)
$$

Now let

$$

p_k := v_k^T X v_k, a_k := v_k^T A v_k.

$$

Then vector $\bp$ belongs to the simplex $\{\bp \in \R^n : p_k\geq 0, \, \sum_kp_k=1\}$ and

$$

(A^2X)
= _kp_k\,a_k^2,

(AX)
= _kp_k\,a_k.

$$

Hence

$$
^T ^2f( )\,

_kp_k a_k^2
-( _kp_ka_k)^2
=:q( ).
$$

Observe that the hessian $(\frac{\partial^2 q(\bp)}{\partial p_i \partial p_j}) = -2(a_ia_j)$ is rank-one negative semidefinite, so that $q$ is concave. In order to get an upper bound on $q$, let us maximize it on the simplex. Construct the Lagrangian $q(\bp)-(\sum_k p_k-1)\ell$ whose stationarity conditions at a maximizer $\bp^*$ are
$a^2_i - 2(\sum_k p^*_k a_k) a_i = \ell$. Let us now prove that amongst all maximizers $\bp^*$, we can choose one whose support $\{i : p^*_i > 0\}$ consists of two indices at most. For every index $i$ in the support, it holds $a_i^2-2b\,a_i=\lambda$ where $b:=\sum_kp^*_ka_k$. This is a quadratic equation in $a_i$, with non-negative discriminant $(-2b)^2-4(-\lambda)=4(b^2+\lambda)=4(b^2+a^2_i+2ba_i)=4(b-a_i)^2$ so the equation admits at most two distinct real solutions $\alpha_1,\alpha_2$. This shows that every $a_i$ with $p^*_i>0$ must be one of the (at most) two roots of that quadratic. In other words, the set $\{a_i : p^*_i>0\}$
contains at most two distinct values. Group the indices by which root they take $I_1=\{\,i:a_i=\alpha_1\}$, $I_2=\{\,i:a_i=\alpha_2\}$.
Define $r_1 = \sum_{i\in I_1}p^*_i$, $r_2 = \sum_{i\in I_2}p^*_i\;=\;1-r_1$.
Since all $a_i$ in $I_1$ equal $\alpha_1$, and all in $I_2$ equal $\alpha_2$, one checks
that the maximum $q(\bp^*)=\sum_{k}p^*_k\,a_k^2-(\sum_kp^*_k\,a_k)^2=r_1(\alpha_1)^2+(1-r_1)(\alpha_2)^2-(r_1\alpha_1+(1-r_1)\alpha_2)^2$ depends only on $r_1$ and not on the way it is split among indices. So without loss of generality, and can choose only one index $i_1 \in I_1$ and one index $i_2 \in I_2$ and the corresponding vector $\bp^*$ will achieve the same maximum on the simplex.
Writing $p^*_{i_1}=t$, $p^*_{i_2}=1-t$ one finds $q(\bp^*)=\max_{t\in[0,1]} t(1-t)(\alpha_1-\alpha_2)^2 = \frac{1}{4}(\alpha_1-\alpha_2)^2$.
Hence on the simplex it holds

$$
q( ) 4( _k a_k - _k a_k)^2.
$$

For any two indices $i$, $j$ it holds $(a_i-a_j)^2 \leq a^2_i+a^2_j \leq 2 \sum_k a^2_k$, and hence $$q( ) 2 a^2_k.$$
Combining our previous bound [ref:boundq]

$$

^T ^2f( )\,
q( )
\; \;
12 _k=1^n a_k^2
\;=\; 12\, (A( )^2)

$$

with the definition [ref:gram] of the Gram matrix

$$

(A( )^2)
= _i,ju_i\,u_j\, (A_iA_j)
= ^T\,G\, ,

$$

we obtain

$$

^T ^2f( )\,
\; \;
12\, ^T\,G\,
\; \;
12\, \,\| \|_2^2.

$$

Since this holds for every direction $\bu$ and every $\by$, it follows that $f$ is $\frac{1}{2}\lambda$-smooth on $\R^m$.

Note that the bound [ref:spectralbound]

$$

(A^2( )\,X( )) -
[ (A( )X( ))]^2
4( _ A( )- _ A( ))

$$

that we just proved algebraically is a particular case of a more general result in non-commutative probability theory, see [cite:BD2000].
}

## Lemma: Strong convexity [label:strongconvexity]

Let $y_0 \in \R^m$ be given. On the sublevel set $ \mathscr S \;:=\;\{\,\by\in\mathbb{R}^m : f(\by)\le f(\by_0)\}$, the function $f$ is $\alpha$-strongly convex with

$$

:= _ (G)n^2 f( _0) > 0.

$$

\new{In particular if $\by_0=0$, $f$ is $\frac{\lambda_{\min}(G)}{n^3}$-strongly convex.}

## Proof [label:alphabound]

First note that $\lambda_{\min}(G) >0$ follows from Assumption [ref:injective].
For the function $$ (X) := X$$
it holds $$ ^2 (X) \; \;
p_ (X)\;I_n,
p_ (X)
:= _i = 1, ,n
_j=1^n _j(X).$$
By the chain rule and the variational form of the Hessian of $\phi$, one shows
$\nabla^2 f(\by) = \mathcal A\, \bigl[\nabla^2\phi(A(\by))\bigr]\,\mathcal A^T$.
Since $\mathcal A\,I_n\,\mathcal A^T=G$, it follows that $$ ^2f( ) _ (G)\;p_ (A( ))\;I_m$$.

Let $\lambda_{\min}(X)\le\cdots\le\lambda_{\max}(X)$ be the eigenvalues of $X$. Then

$$

p_ (X)
= _ (X) _j _j(X)
\; \;
_ (X)n\, _ (X)
\;=\;
n ( _ (X)- _ (X)).

$$

Define the spectral‐gap over $\mathscr S$:

$$

:=
_ S
_ (A( ))- _ (A( )) .

$$

Then for all $\by\in \mathscr S$, it holds $p_{\min}(A(\by)) \ge
\frac{1}{n\exp\delta}$,
and hence $\lambda_{\min}(\nabla^2f(\by)) \ge
\lambda_{\min}(G)\;\frac{1}{n\exp\delta}$.
Taking the infimum over $\by\in \mathscr S$ yields

$$

= _ S _ ( ^2f( ))
\; \;
_ (G)n .
$$

By strong duality for the log‐partition function,
$$f( ^*) = _ f( )
\;=\;
_X ^n_1
X X
\; \;
- n.
$$
On the other hand, since $f$ increases with the spectral‐gap,
$
\delta
\;\le\;
f(\by_0)-f(\by^*)
\;\le\;
f(y_0)+\log n.
$
Therefore
$
\exp\delta
\;\le\;
\exp(f(\by_0)+\log n)
\;=\;
n\exp f(\by_0).
$
Substituting into [ref:alphabound] gives the required result.

## Pre-conditioning

For the conditioning $\kappa$ of $f$ to be as small as possible, Lemmas [ref:smoothness] and [ref:strongconvexity] indicate that $\lambda_{\max}(G)$ should be small and $\lambda_{\min}(G)$ should be large, where $G$ is the Gram matrix [ref:gram] of the $A_i$. To accelerate the convergence of optimization algorithms to minimize $f$, we wish to replace $A_i$ by a new set $\hat{A}_i$ whose Gram matrix is perfectly conditioned, i.e. $\lambda_{\max}(G) = \lambda_{\min}(G) = 1$ or equivalently $G = I_m$.

\begin{algorithm}[H]
\caption{Pre-conditioning}{#algo}
\begin{algorithmic}[1]
\REQUIRE $A_1,\dots,A_m \in \S^n$\\
\ENSURE $\hat A_1,\dots,\hat A_m \in \S^n$ with $\Tr\hat A_i=0$ and $\Tr(\hat A_i\hat A_j)=\delta_{i-j}$
\STATE {\bfseries (Center to traceless)}

$$

A'_i \;=\; A_i \;-\; n\,I_n,
i=1, ,m.

$$

\STATE {\bfseries (Compute Gram matrix)}

$$

G'_ij \;=\; (A'_i\,A'_j ),
i,j=1, ,m.

$$

\STATE {\bfseries (Whitening)}
Compute the symmetric inverse‐square‐root $G'^{-1/2}$ via eigendecomposition:

$$

G' = U\,D\,U^T,

G'^-1/2 = U\,D^-1/2\,U^T.

$$

\STATE {\bfseries (Form Orthonormal Basis)}

$$

A_i
= _j=1^m (G'^-1/2 )_ij\;A'_j,
i=1, ,m.

$$

\end{algorithmic}
\end{algorithm}

## Theorem: Correctness

The output $\hat A_i$ of Algorithm [ref:algo] satisfies:

- $\displaystyle\Tr\hat A_i=0$ for all $i$.

- $\displaystyle\Tr(\hat A_i\,\hat A_j)=\delta_{i-j}$ for all $i,j$.

Hence the $\hat A_i$ are traceless and orthonormal.

## Proof

1. Tracelessness. Each $A'_i$ is by construction

$$

A'_i
= A_i - n I_n
= A_k - A_k
=0.

$$

Since $\hat A_i$ is a linear combination of the $A'_j$, it too is traceless:
$\Tr \hat A_i=\sum_j(G'^{-1/2})_{ij}\,\Tr A'_j=0.$

\medskip\noindent
2. Orthonormality. Define the centered Gram matrix $G'$. Then

$$

( A_i\, A_j)
= _p,q(G'^-1/2)_ip\,(G'^-1/2)_jq\,
(A'_p\,A'_q)
= _ij
= _i-j.

$$

This shows $(\hat A_i)$ are orthonormal in the Frobenius inner product.

## Example

Let $n=3$, $m=2$ and

$$

A_1 =
( rrr
6 & 1 & 0 \\ 1 & 2 & 0 \\ 0 & 0 & -2
),
A_2 =
2 ( rrr
-2 & 1 & 0 \\ 1 & 2 & 0 \\ 0 & 0 & 6
)

$$

whose Gram matrix has eigenvalues $28$ and $64$. Step 1 of Algorithm [ref:algo] yields the traceless matrices

$$

A'_1 =
( rrr
4 & 1 & 0 \\ 1 & 0 & 0 \\ 0 & 0 & -4
),
A'_2 =
2 ( rrr
-4 & 1 & 0 \\ 1 & 0 & 0 \\ 0 & 0 & 4
)

$$

whose Gram matrix computed in step 2 has eigenvalues $4$ and $64$. Finally, step 4 yields the traceless orthonormal matrices

$$

_1 =
2 ( rrr
1 & 1 & 0 \\ 1 & 0 & 0 \\ 0 & 0 & -1
),
_2 =
2 ( rrr
-1 & 1 & 0 \\ 1 & 0 & 0 \\ 0 & 0 & 1
)

$$

corresponding to Example [ref:ex:2d], whose Gram matrix is identity. The corresponding moment bodies, before and after the application of Algorithm [ref:algo], are represented in Figure [ref:fig:momentbodycond]. The same pre-conditioned moment body is also represented on Figures [ref:fig:momentbody2d] and [ref:fig:momentbodygrid2d].

We remark that the idea of using the Cholesky factor of the Gram matrix of the linear map was already exploited in the context of projection methods for semidefinite optimization, see [cite:mprw09] and [cite:hm12].

## Refined geometric analysis

In this section we assume that after the application of Algorithm [ref:algo] the matrices $A_i$ are traceless and orthonormal, i.e.

$$

A_i = 0, (A_i A_j) = _i-j, i,j=1, ,m.

$$

or equivalently $\mathcal A(I_n)=0$, $\mathcal A \circ \mathcal A^T = I_m$. The corresponding moment body $\mathscr M$ is normalized\footnote{A Traceless Orthonormal Moment Body can be called a TOMB, evoking a solid, well-defined shape, where all the traceless moments rest.}, and we now report some of its geometric properties.

## Lemma [label:radius]

The radius of $\mathscr M$ is

$$

rad\, M:= _X ^n_1\|A(X)\|_2
\;=\;
n < 1.

$$

## Proof

Since $A_i$ are traceless and orthonormal, it holds $\mathcal A(\frac{1}{n}I_n)=0$ and hence $\mathscr M$ is centered at the origin. \new{The map $\mathcal A$ acts on the traceless part of $X$:

$$

A(X) = A(X- nI_n).

$$

Furthermore, since the map $\mathcal A$ (from the space of matrices with the Frobenius norm to $\R^m$ with the Euclidean norm) corresponds to an orthogonal projection, its operator norm is at most one. This implies the inequality $\|\mathcal A(Y)\|_2 \le \|Y\|_F$ for any $Y$. Hence, for any matrix $X \in \S^n_1$:

$$

\| A(X)\|_2^2 = \| A(X - 1n I_n)\|_2^2 \|X - 1n I_n\|_F^2.

$$

The radius is therefore bounded by the maximum value of the term on the right. We have

$$

\|X - 1n I\|_F^2 = \|X\|_F^2 - n X + n 1 - n + n = n,

$$

where we used $\|X\|_F^2=\trace(X^2)\le \trace(X)=1$. This proves that $rad\,\mathscr M \le \sqrt{\frac{n-1}{n}}$. As this bound is achievable, the equality holds.
}

The support function of $\mathscr M$ is $h : \mathscr U \to \R_+, \bu \mapsto h(\bu):=\sup_{\bx \in \mathscr M} \bb^T \bx$.
The width of $\mathscr M$ in direction $\bu$ is $w(\bu):=h(\bu)-h(-\bu)$. The minimal width or thickness, is $thick\:\mathscr M:=\inf_{\bu \in \mathscr U}w(\bu)$. The maximal width or diameter, is $diam\:\mathscr M:=\sup_{\bu \in \mathscr U}w(\bu)$.

## Lemma

It holds
$h(\bu) = \lambda_{\max}(A(\bu))$
and hence
$w(\bu) = \lambda_{\max}(A(\bu))
-\lambda_{\min}(A(\bu))$
is the spectral gap along direction $\bu$.
Moreover

$$

diam\, M= ,

thick\, M = n/2 \, n/2 = ll
& n=2k,\\
k(k+1) & n=2k+1.
.

$$

## Proof

{\bf~(Sketch)}
For any unit vector $\bu \in \mathscr U$,

$$

_ M ^T = _X ^n_1 ^T A(X)
= _X ^n_1 (A( )X)
= _ (A( ))

$$

and similarly for the infimum.
The maximal spectral gap of a traceless Frobenius‐unit matrix is achieved by a rank‐2 matrix with eigenvalues $\pm\tfrac1{\sqrt2}$, giving $\sqrt2$. The minimal spectral gap occurs when the positive and negative eigenvalues are as evenly distributed as possible, leading to the stated formula in terms of $\lfloor n/2\rfloor$ and $\lceil n/2\rceil$.

We observe that the diameter $\sqrt{2}$ is less than twice the radius $2\sqrt{\frac{
n-1}{n}}$, reflecting the fact that $\mathscr M$ is not centrally symmetric.

Let
$$
^* := _ ^m f( )
$$
denote the minimizer of $f$, which is unique from Lemma [ref:unique].
The geometric properties of $\mathscr M$ allow us to bound the value of $f$ at $\by^*$, as well as the norm of $\by^*$ itself.
Let \new{Assumption [ref:interior] hold for the remainder of this section, and let}

$$

:= 1 - \| \|_2 (0,1].

$$

\new{We can bound the minimum and the norm of the minimizer. Tighter bounds can be obtained, but their expressions are slightly more involved.}

## Lemma [label:bounds]

It holds

$$

\; \;f( ^*)\; \; n,

\| ^*\|_2\; \; \; .

$$

## Proof

Since $\bb\in \mathscr M$ and by Lemma [ref:radius] the largest norm of any point in $\mathscr M$ is $rad\,\mathscr M =
\sqrt{\tfrac{n-1}{n}}<1$, it follows that $\beta>0$.
Moreover, the distance of $\bb$ to the boundary of $\mathscr M$ is larger than the distance of $\bb$ to the unit sphere $\mathscr U$, equal to $\beta$.
By strong duality,

$$

f( ^*)
= _X ^n_1 (X X)
\; \;
\! _ (X^*)
\; \;
.

$$

On the other hand, evaluating at $y=0$ gives

$$

f( ^*)\; \;f(0)
= A(0)= n.

$$

Hence the first two inequalities.

Next, since $\mathcal A^T$ is an isometry onto the traceless subspace, it holds
$\|\by^*\|_2=\|A(\by^*)\|_F$. But

$$

A( ^*)
= X^*- 1n X^*\,I_n,

$$

and the spectrum of $X^*$ lies in $[\beta,\;1]$. Therefore each eigenvalue of $\log X^*$ lies in $[\log\beta,0]$, so the centered spectrum lies in an interval of length $-\log\beta$.
Hence

$$

\| ^*\|_2^2=\| X^*- 1n X^*\,I_n\|^2_F= _i( _i- 1n _i _i)^2 n\, (- )^2.

$$

## Theorem [label:smoothrefined]

Function $f$ is $\frac{1}{2}$-smooth.

## Proof

\new{
The global smoothness constant is just an application of Lemma [ref:smoothness] when $G=I_m$.}

## Theorem [label:strongconvexityrefined]

\new{Function $f$ is $\frac{1}{n^3}$-strongly convex and $\beta$-strongly convex around its minimizer.}

## Proof

The sublevel strong convexity modulus is just an application of Lemma [ref:strongconvexity] when $G=I_m$ and $\by_0=0$, \new{since then $\exp f(\by_0)=n$. As shown in the proof of Lemma [ref:bounds]}, at the minimizer $\by^*$, it holds
$\lambda_{\min}(X(\by^*))\ge \beta$, and hence
$\nabla^2f(\by^*)\succeq\lambda_{\min}(X(\by^*))I_m\ge\beta I_m.$

## Lemma

On the sublevel set
$\{\by:\,f(\by)\le f(\by_0)\}$,
all eigenvalues $\lambda_i( A(\by))$ are uniformly bounded:

$$

| _i(A( ))|
\; \;
n(f( _0)+ n)

$$

and in particular if $\by_0=0$ this simplifies to

$$

| _i(A( ))|
\; \;
n.

$$

## Proof

The sum of the eigenvalues of $A(\by)$ is zero:
$\trace A(\by)=\sum_{i=1}^n \lambda_i(A(\by))=0$.
Define the spectral gap
$\delta(\by):=\lambda_{\max}(A(\by))-\lambda_{\min}(A(\by))$.
By standard log‐partition duality one shows
$\delta(\by)\;\le\;f(\by)-f(\by^*) \;\le\;f(\by_0)-\min f
\;\le\;f(\by_0)+\log n,
$ using $f(\by^*)\ge-\log n$ for the minimizer $\by^*$. Hence for all $\by$ in the sublevel set $\{\by:\,f(\by)\le f(\by_0)\}$, it holds $\delta(\by)\le f(\by_0)+\log n$.
Moreover, from the zero‐trace condition
$0=\sum_{i=1}^n\lambda_i=\ge\lambda_{\max}+(n-1)\lambda_{\min}$ so $\lambda_{\max}(A(\by))\le-(n-1)\,\lambda_{\min}(A(\by))$.
Combine with $\delta=\lambda_{\max}-\lambda_{\min}$ to get $
\lambda_{\max}\le\frac{n-1}{n}\,\delta$ and $
\lambda_{\min}\ge-\frac{n-1}{n}\,\delta$.
Substituting the upper bound on $\delta$ yields the claimed uniform spectral bound.

## Complexity analysis

Now we are fully equipped to analyse the convergence and computational complexity of L-BFGS for minimizing $f$
with normalized data.

## Theorem

Under Assumption [ref:interior],
let $\by_k$ denote the L-BFGS iterates (with exact line‐search). Given $\epsilon > 0$, in order to guarantee $\|\nabla f(\by_k)\|\le\epsilon$ it suffices to take

$$

k n^2 f( _0) ( ).

$$

In particular if $\by_0=0$ this simplifies to

$$

k n^3 ( ).

$$

## Proof

Since $f$ is $\alpha$-strongly convex and $\lambda$-smooth on $\R^m$, a standard result (e.g.\ for gradient descent with step $1/\lambda$) gives
$f(\by_{k+1}) - f(\by^*)
\;\le\;
(1-\frac{\alpha}{\lambda})(f(\by_k)-f(\by^*))$,
and by induction
$f(\by_k)-f(\by^*)
\;\le\;
(1-\frac{\alpha}{\lambda})^k
(f(\by_0)-f(\by^*)).
$
Moreover using the inequality $1-t\le e^{-t}$ for $0<t<1$, we obtain
$
f(\by_k)-f(\by^*)
\;\le\;
\exp(-\frac{\alpha}{\lambda} k)(f(\by_0)-f(\by^*)).
$
By Lemma [ref:bounds] we have the bound
$f(\by^*) \geq \log\beta$, so
$f(\by_0)-f(\by^*) \leq f(\by_0) - \log\beta$. Hence $f(\by_k)-f(\by*) \leq \exp(-\frac{\alpha}{\lambda} k)(f(\by_0)-\log\beta)$.
By $\lambda$-smoothness, for any $\by$ we have
$\|\nabla f(\by)\|_2^2 \leq 2\lambda(f(\by)-f(\by^*))$.
Applying this at $\by=\by_k$ gives
$\|\nabla f(\by_k)\|_2 \leq \sqrt{2\lambda(f(\by_k)-f(\by^*))} \leq \sqrt{2\lambda(f(\by_0)-\log\beta)}\exp(-\frac{\alpha}{2\beta}k).$
To ensure $\|\nabla f(\by_k)\|_2\le\epsilon$, we require
$k \geq \frac{2\lambda}{\alpha}\log(\frac{1}{\epsilon}\sqrt{2\lambda(f(\by_0)-\log\beta)})$.
The final expressions are obtained by letting $\lambda=\frac{1}{2}$ (Theorem [ref:smoothrefined]) and $\alpha=\frac{1}{n^2}\exp(-f(\by_0))$ (Theorem [ref:strongconvexityrefined]).

## Theorem

The cost of one iteration for L-BFGS with memory $m_{hist}$ and exact line-search is $O(n^3+mn^2+m_{hist}^2)$.

## Proof

Each iteration involves:

- {Matrix exponential and normalization:} diagonalize $A(\by)$ in $O(n^3)$ to form $X = \exp_1 A(\by)$.

- {Gradient evaluation:} compute $A(X)-\bb$, requiring $m$ inner‐products $\trace(A_iX)$ at $O(n^2)$ each, total $O(mn^2)$.

- {Two‐loop recursion:} update the L‑BFGS direction in $O(m_{hist}^2)$.

## Feasibility versus infeasibility

Let us now relax Assumption [ref:interior] and distinguish two cases.

### Weak feasibility

Weak feasibility means $\bb \notin \Int \mathscr M$ but $\bb \in \mathscr M$, i.e. $\bb$ lies along the boundary of the moment body.
Then $f$ remains convex and finite for all $\by$. It grows in $O(\log \by)$
along the unique supporting direction $\bu$ with $\lambda_{\max}(A(\bu))=\bb^T\bu$, and linearly in all other directions. The Hessian of $f$ is positive semidefinite but degenerates as $\|\by\|\to\infty$ in direction $\bu$.
No finite minimizer exists and L-BFGS iterates drift off along $\bu$. The gradient norm decays only in $O(1/\|\by\|)$, so convergence stalls. Sublevel sets $\{\by : f(\by) \leq f(\by_0)\}$ are unbounded in the direction $\bu$.

## Corollary

Fix a tolerance $\epsilon>0$. At iteration $k$, if $\|\nabla f(\by_k)\|_2 \;\le\;\epsilon$,
then setting $X_k=\exp_1 A(\by_k)$ yields a matrix $X_k \in \S^n_1$ satisfying $\|A(X_k)-b\|_2
\;\le\;
\epsilon$, therefore certifying that $\bb$ lies within distance $\epsilon$ of $\mathscr M$.

## Proof

From Lemma [ref:gh] it holds $\nabla f(\by)=A(X(\by)) - \bb$, independently of the location of $\bb$.
Hence

$$

\| f( )\|_2
= \|A(X( )) - \|_2

_X 0,\; X=1\|A(X)- \|_2
= _ M \| - \|_2

$$

since $X(\by)$ is one particular feasible point in the minimum defining the distance of $\bb$ to $\mathscr M$.
In particular, if $\|\nabla f(\by)\|_2\le\varepsilon$ then
$ \min_{\bx \in \mathscr M} \|\bx-\bb\|_2\le\varepsilon$, i.e.\ $b$ lies within $\varepsilon$ of $\mathscr M$.

## Corollary

At iteration $k$, if $\|\by_k\| > \sqrt{n}\log\frac{1}{\beta}$, then $\bb \notin \Int\mathscr M$.

## Proof

By Lemma [ref:bounds], any interior feasible $\bb$ forces the sublevel set $\{\by : f(\by) \leq f(\by_0)\}$ to lie inside the ball $\{\by : \|\by\|\le \sqrt{n}\log\frac{1}{\beta}\}$.

### Infeasibility

If $\bb \notin \mathscr M$ then $f$ is convex and unbounded below. There exists $\bu \in \mathscr U$ so that for all large $t$, $f(t\bu)\approx t(\lambda_{\max}(A(\bu))-\bb^T\bu \to-\infty$.

## Lemma [label:separation]

If $f(\by)<0$ for some $\by \in \R^m$, then $b \notin \mathscr M$ and the vector $\by/\|\by\| \in \mathscr U$ is a certificate of infeasibility.

## Proof

The support function of $\mathscr M$ is $h(\bu):=\max_{X \in S^n_1} \bu^T A(X) = \lambda_{\max}(A(\bu))$.
By convex separation:
$\bb\notin\mathscr M$ if and only if $\bb^T \bu > \lambda_{\max}(A(\bu))$.
On the other hand, since $\log\trace\exp X \geq \lambda_{\max}(X)$ for all $X \in \S^n$, we have
$f(\by)=\log\trace\exp A(\by) - \bb^T \by \geq \lambda_{\max}(A(\by)) - \bb^T \by$.
Hence if $f(y)<0$ then $\bb^T \by - \lambda_{\max}(A(\by)) > 0$ and the normalized vector $\by / \|\by\| \in \mathscr U$ yields a strict separating hyperplane certifying $b\notin M$.

## Corollary

If $\bb \notin \mathscr M$, then
$\inf_{\by}f(\by)=-\infty$. Moreover, when L-BFGS with exact line-search is applied to $f$, the iterates satisfy
$f(\by_{k+1})<f(\by_k)$ for all $k$, and hence there exists a finite index $k$ for which $f(\by_k)<0$.

## Proof

Since $\bb\notin \mathscr M$, by Lemma [ref:separation] there is a vector $\bu$ with $\lambda_{\max}(A(\bu))<\bb^T \bu$, whence $f(\bu) \leq \lambda_{\max}(A(\bu))-\bb^T \bu < 0$ and $\sup_{t>0}f(t\bu)\to-\infty$, so $\inf f=-\infty$.

Under exact line-search L-BFGS is a descent method: at each step,
provided $\nabla f(\by_k)\neq0$, the value of $f$ strictly decreases: $f(\by_{k+1})<f(\by_k)$. From Lemma [ref:gh], no stationary point exists when $\bb\notin \mathscr M$, so $\nabla f(\by_k)\neq0$ for all $k$. Hence $f(\by_k)$ is strictly decreasing and unbounded below. Since $f(\by_0)$ is finite, there must be some finite $k$ at which $f(\by_k)$ crosses zero, i.e.\ $f(\by_k)<0$.

## Corollary

At iteration $k$, if $f(\by_k) < 0$ then $\by_k /\|\by_k\|$
is a certificate of infeasibility implying $\bb\notin \mathscr M$, and the algorithm may terminate.

Finally, straightforward sufficient conditions for infeasibility can be derived from Lemma [ref:radius]: if $\|\bb\| > \frac{n-1}{n}$ then $\bb \notin \mathscr M$. Similarly, componentwise if $b_i \notin [\min_{X \in \S^n_1} \trace(A_iX), \max_{X \in \S^n_1} \trace(A_i X)] = [\lambda_{\min}(A_i), \lambda_{\max}(A_i)]$ for some $i=1,\ldots,m$ then $\bb \notin \mathscr M$.

## Block-separable problems

We can
consider a block separable version of the maximum entropy primal

$$

_X\; & _j=1^p
\\

& _j=1^p (A_ij\,X_j ) \;=\; b_i
i=1, ,m,\\
&X _j=1^p ^n_j ^ _j=1^p n_j_1

$$

where the unknown matrix $X$ is block diagonal with positive semidefinite blocks $X_j \in \S^{n_j}$ whose traces sum up to one. Its Lagrangian separates over $j$, and as in the proof of Theorem [ref:primaldual] one shows that the dual can be written as the unconstrained minimization

$$

_ ^m

_j=1^p
_i=1^m y_i\,A_ij - ^ .

$$

## Numerical experiments

We constructed a basic Matlab implementation\footnote{Available for download at \hyperlink{https://homepages.laas.fr/henrion/software/maxentmom/maxentmom.m}{homepages.laas.fr/henrion/software/maxentmom/maxentmom.m}} of L-BFGS that takes as input matrix $A$ of size $m$-by-$n^2$ and a vector $\bb$ of size $m$, and returns a vector $\by$ of size $m$ minimizing $f$:

The algorithm calls the following function which evaluates $f$ and its gradient:

Alternatively we can use HANSO [cite:hanso] which is a Matlab implementation of L-BFGS also aimed at non-smooth non-convex problems.

Convergence of iterate $\by_k$ occurs when the norm of the residual $\mathcal{A}(\exp_1 \mathcal A^T(\by_k))-\bb$ (i.e. the gradient of $f$ at $\by_k$) is smaller than some a priori given expected accuracy, typically $10^{-8}$.
This is a relative accuracy when the data is normalized via Algorithm [ref:algo], since the norm of $\bb$ is less than one by Lemma [ref:radius].

### Toy problem

Let us illustrate the behavior of {\tt maxentmom} on our toy planar moment body of Example [ref:ex:2d]. On Figure [ref:fig:iterates2d] are represented 10 typical trajectories $\mathcal A(\exp_1 \mathcal A^T(\by_k))$ for 10 different target vectors $\bb$ chosen close to the boundary of the moment body, with the same initial condition $\by_0 = 0$. Iterates are represented by black dots, and typically 7 iterations suffice to reach the target vector at accuracy $10^{-8}$.

### Medium scale problems

Our implementation largely outperforms the state-of-the-art second-order interior-point semidefinite solver of MOSEK. For example, on our standard laptop, with $m=n=300$ and accuracy $10^{-8}$, {\tt maxentmom} takes 0.2s and 7 iterations to solve a random problem, compared to 180s with MOSEK.
Random problems are generated as follows. We apply pre-conditioning algorithm [ref:algo] on a normally distributed random map $A$, we let $X$ be the normalized exponential of a normally distributed random symmetric matrix, and we choose $\bb=\mathcal A(X)$.

### Larger scale problems

Our rudimentary implementation stands the comparison with SDPNAL+1.0, a state of the art large-scale semidefinite solver based on semismooth Newton-CG augmented Lagrangian [cite:sdpnal15,sdpnal20], see Figure [ref:fig:maxentmom_sdpnal] which corresponds to normalized randomly generated instances as described in the previous section. For $m=n=1000$ and expected accuracy $10^{-8}$, {\tt maxentmom} solves a randomly generated moment body membership problem in less than 5s.
For larger problems, it is too costly to store the linear map as a single matrix $A$ of size $m$-by-$n^2$, and other storage and matrix vector multiplication strategies must be followed. For illustration, when $m=n=1000$, storing a double precision matrix $A$ requires almost $8$ gigabytes.

## Conclusion

Motivated by pre-conditioning strategies for semidefinite optimization, this paper reports on a specific problem class whose geometry is simple enough to allow for a comprehensive analysis. We consider the moment body membership oracle problem, which consists of determining whether a given vector of size $m$ belongs to a given linear projection of the spectraplex, the compact convex set of unit trace positive semdefinite matrices of size $n$-by-$n$. Inspired by maximum entropy techniques from quantum information theory, we propose to solve the problem by minimizing on the whole $m$-dimensional space a dual smooth strictly convex log-partition function. Geometric curvature analysis reveals how key input data quantities can be modified to improve the problem conditioning. After pre-conditioning, we can solve the convex dual problem with L-BFGS, a widely used first-order algorithm approximating second-order information with limited gradient evaluation and storage. Numerical experiments on a rudimentary Matlab implementation show that the approach largely outperforms second-order interior-point methods, while standing the comparison with state-of-the-art first-order algorithms for large-scale semidefinite optimization. Fully dense (i.e. non-sparse) problems of size $n=m=1000$ can be solved to 8 significant digits in a few seconds on a standard laptop, the only limitation being the memory requirements. For this problem class, it means that the bottleneck is now essentially concentrated into efficient gradient computation and storage, consistently with the recent developments reported e.g. in [cite:sketchy, hallar].

Polynomial SOS decompositions are particular cases of the moment body membership oracle, where $X$ is the Gram matrix representing a polynomial $p(\bx) = \phi^T(\bx) X \phi(\bx)$ as a quadratic form w.r.t. some basis vector $\phi$. The linear map $\mathcal A(X)=\bb$ matches $X$ with $p$ expressed as a coefficient vector $\bb$ in some basis.
It can be normalized since $\int p(\bx) d\mu(\bx) = \trace (X\int \phi(\bx) \phi^T(\bx) d\mu(\bx)) = \trace X$
whenever $\phi$ is an orthonormal basis with respect to the inner product induced by $\mu$. Memberships in truncated quadratic modules, also called weighted SOS decompositions, can also be modeled as particular moment body problems. They are at the core of the moment-SOS hierarchy for polynomial optimization [cite:hkl20]. It would be interesting to derive specific curvature properties to pre-condition these problems in the same way we did it for general moment bodies. Relationships with the dual certificates of truncated quadratic module membership investigated in [cite:dp22] are also worth investigating, especially since these dual certificates allow to construct SOS representations with rational coefficients.

In the context of semidefinite relaxations of combinatorial optimization problems, the trace one constraint holds for the first relaxation of the moment-SOS hierarchy. This constant trace property was exploited in [cite:hr00] in the context of spectral bundle methods. It was generalized in [cite:mlm23,mlmw22] where it was shown that every every polynomial optimization problem on a compact semialgebraic set has an equivalent equality constrained formulation on an sphere (possibly after adding some artificial variables), and hence a constant trace moment relaxation.

A natural extension of our approach consists of minimizing a linear function on the moment body, i.e. given a matrix $C \in \S^n$, solving the semidefinite optimization problem
$$
_X ^n_1 (CX) .t. A(X) = .
$$
For a given regularization parameter $\mu>0$, to a primal entropic problem
$$
_X ^n_1 (CX) - \: (X - X X) .t. A(X) =
$$
corresponds a dual log-partition problem
$$
_ ^m ^T - \; (- (C - A( ))).
$$
One then follows a primal admissible central path
$$
X^*_ = _1(- (C-A( ^*_ ))) A^-1( )
$$
parametrized by dual optimal solutions $\by^*_{\mu}$
and we let $\mu \to 0^+$. A detailed analysis of convergence of this method remains to be done. Note that the idea was followed recently in [cite:l23,cl25], but without the trace-one restriction. Consequently, the dual function there is the much less regular partition function, which is the exponential of the log-partition function. This may explain why the semidefinite optimization experiments reported in [cite:cl25] are somewhat disappointing. Whether more convincing and scalable numerical results can be obtained with the log-partition function remains however to be seen.

## Acknowledgement

Solving the spectrahedral shadow membership with a first order optimization algorithm was suggested to the author by Stephan Weis at Mathematisches Forschungsinstitut Oberwolfach in August 2024. He also pointed out references [cite:h24,jv23] A significant part of this work was done during a stay at the Institute of Pure and Applied Mathematics of the University of California at Los Angeles, whose hospitality has been appreciated.
This work benefited from feedback from Saroj Prasad Chhatoi, Jean Bernard Lasserre, Victor Magron, .
