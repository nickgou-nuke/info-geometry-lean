# 68. Operator-Algebraic Information Geometry as Singular Decomposition

*Date: April 14, 2026*  
*Context: Type III state-first doctrine, modular replacement of traces, and Lean chunk translation*

## Core Lock

For the Spire codebase, local quantum information geometry is implemented as:

- state-first (`ω`) and modular-first (Tomita/relative modular),
- not trace-first (`Tr(ρ ·)`),
- with singular decomposition handled by Drazin/Moore-Penrose projector lanes.

In short: **type III-compatible operatorial geometry, not finite-trace shadow geometry**.

## Why This Is Canonical in Repo Terms

The current owner spine already supports the required language:

- `CertifiedInverseKernel` / projector algebra for regular vs apex lanes,
- `RelativeModularScaleShapeSplit` for block decomposition of bounded modular representatives,
- `SingularDecompositionSurrogate` for anomaly/supercharge closure packaging,
- `OperatorialCramerRao` for noncommutative Cramér-Rao bounds on perturbation channels.

This means the doctrine is now translation work, not ontology invention.

## Translation Program (Chunked)

### Chunk 1 (implemented now)

Build a state-first bridge package that combines:

1. wedge-calibrated modular split (active/apex),
2. singular closure package (supercharge commutator form),
3. operatorial Cramér-Rao lower bound on comparison channels.

This is the minimum executable bridge from “type III modular state” to
“information bound + singular projector closure”.

### Chunk 2

Refine the same package with explicit non-vacuity edge cases:

- apex-zero degeneration (`Q₀ = 0`) reduces to active block,
- commutation witness path made explicit as reusable interface.

### Chunk 3

Integrate a measurable/admissible operator gate as a conservative predicate
layered above state/modular ownership, not replacing it.

## Hard Rule

No chapter-level metaphysics may bypass owner surfaces.

Every claim must compile through:

- modular/state owner,
- projector/singular owner,
- transport/information-bound owner.

This chapter is therefore a translation contract:
the doctrine is valid exactly where the Lean surfaces already prove it.

