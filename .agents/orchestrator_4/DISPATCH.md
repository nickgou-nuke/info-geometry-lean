## 2026-09-22T03:54:20Z

<USER_REQUEST>
You are the Project Orchestrator (orchestrator_4) for the repository /home/goutev/info-geometry-lean.
Your working directory is: /home/goutev/info-geometry-lean/.agents/orchestrator_4/

Authoritative user request is located at:
/home/goutev/info-geometry-lean/.agents/ORIGINAL_REQUEST.md

Mission:
Execute Surgical Refactoring & Compression Swarm (BASH-ONLY MODE) to eliminate the worst `native_decide` compiler bottlenecks remaining in the repository.

Core Mandates:
1. Surgical Precision, No Mass Changes: Mass global sed rewrites across the 588 files are strictly unacceptable. You must identify and target ONLY the highest-impact bottlenecks requiring computational compression.
2. Subagent Sandbox Mandate: ALL file modifications MUST be generated, written, and compiled inside isolated sandbox environments (e.g., .agents/sandbox_name/) first. Do not touch live repository files until the sandbox mathematical fidelity is verified.
3. OpenGauss Synergy: Leverage the OpenGauss workflows (/golf, /refactor) and the lean-lsp-mcp tools to generate the O(1) certificates.
4. BASH-ONLY Security Kernel Bypass: You and all subagents are STRICTLY FORBIDDEN from using the Antigravity write_to_file or replace_file_content tools due to UI deadlocks. You must use run_command with bash (cat << 'EOF', sed, echo) for ALL file writes.
5. QMS Protocol & Safe Lake Build: Maintain continuous git tracking (git add -A) after every file creation/edit. Adhere to the sequential build locks using python3 tools/infra/run_locked_lake_build.py --wait-for-build-lock. NEVER run lake clean or delete build cache.

Instructions:
- Initialize your BRIEFING.md, plan.md, and progress.md in /home/goutev/info-geometry-lean/.agents/orchestrator_4/ using bash (run_command).
- Stage all creations/edits with `git add -A`.
- Execute Phase 0 bottleneck prioritization to locate the worst remaining native_decide compiler bottlenecks.
- Spawn specialist subagents (explorers, workers, reviewers) into dedicated subdirectories under .agents/ with isolated sandboxes.
- Ensure all candidate replacements are tested and verified inside sandboxes before surgical promotion to live owner files.
- Provide regular updates in progress.md and report completion when the compression pass is verified and ready for Victory Audit.
</USER_REQUEST>
