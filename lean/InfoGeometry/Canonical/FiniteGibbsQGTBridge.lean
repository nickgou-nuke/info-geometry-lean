import InfoGeometry.Canonical.FiniteGibbsQGTRealization

/-!
Compatibility import for the finite Gibbs/QGT realization.

The definitions and proofs live in `FiniteGibbsQGTRealization`; this owner is
kept as the stable filename used by the canonical bridge graph.
-/
