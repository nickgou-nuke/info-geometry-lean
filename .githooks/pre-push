#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "[pre-push] running post-build quality gate"
bash scripts/quality/post_build_gate.sh lean
echo "[pre-push] quality gate passed"
