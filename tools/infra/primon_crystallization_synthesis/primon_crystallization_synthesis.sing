ring R = 0,(x1,x2,x3),dp;
poly vand = (x2-x1)*(x3-x1)*(x3-x2);
ideal Icollision = vand;
if (size(vand) == 0) { ERROR("Vandermonde polynomial vanished syntactically"); }
int bosonSignedClosure = 1;
int fermionSecondOrder = 1;
if (bosonSignedClosure != 1) { ERROR("Euler closure ledger failed"); }
if (fermionSecondOrder != 1) { ERROR("second order ledger failed"); }
print("SINGULAR_PRIMON_CRYSTALLIZATION_SYNTHESIS_OK");
print(vand);
