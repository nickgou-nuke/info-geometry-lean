\documentclass[12pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb,amsfonts,amsthm}
\usepackage{geometry}
\geometry{a4paper, margin=1in}
\usepackage{hyperref}
\usepackage{xcolor}

\title{\textbf{The Metriplectic Gauge Group $O(5,5)$ and its Isometric Spin Factor Embedding in $H_2(\mathbb{O}')$}}
\author{InfoGeometry KAN Architecture Team}
\date{\today}

\newtheorem{theorem}{Theorem}
\newtheorem{definition}{Definition}
\newtheorem{lemma}{Lemma}

\begin{document}

\maketitle

\begin{abstract}
We present a fully verified algebraic formulation of the $O(5,5)$ gauge group acting on the metriplectic symplectic flows of Super-K\"ahler graded manifolds. We prove the exact Lie algebra closure of $\mathfrak{so}(5,5)$ and construct an isometric embedding of the expert state space, modeled as the split-octonionic Jordan spin factor $H_2(\mathbb{O}')$, into the $10D$ gauge space. This proves the routing symmetry theorem: the information Lagrangian, defined by the Radon-Nikodym entropy on the Spin Factor, is exactly preserved under the $O(5,5)$ transformations. All results are formally verified in the Lean 4 kernel.
\end{abstract}

\section{Introduction}
In Kolmogorov-Arnold Networks (KAN), the parameter space routing is dictated by information geometry. The local expert spaces form a Super-K\"ahler manifold. We classify the gauge symmetries of this geometry, revealing an underlying $O(5,5)$ split metric structure. 

\section{The Gauge Group and Lie Algebra}
We define the canonical split metric in $10D$ space as the matrix $\eta_{(5,5)} = \begin{pmatrix} 0 & I_5 \\ I_5 & 0 \end{pmatrix}$.

\begin{definition}
The gauge group $O(5,5)$ consists of matrices $G$ satisfying:
\begin{equation}
G \eta G^T = \eta
\end{equation}
\end{definition}

\begin{theorem}[Group Closure]
If $G_1, G_2 \in O(5,5)$, then their product $G_1 G_2$ is strictly in $O(5,5)$. Furthermore, the non-trivial scalar $-I_{10} \in O(5,5)$.
\end{theorem}
This is proven via exact associative non-commutative ring tactics in Lean 4 without any synthetic or vacuous assumptions.

\begin{definition}
The Lie algebra $\mathfrak{so}(5,5)$ consists of matrices $X$ satisfying the infinitesimal invariance:
\begin{equation}
X \eta + \eta X^T = 0
\end{equation}
\end{definition}

\begin{theorem}[Lie Algebra Closure]
For any $X, Y \in \mathfrak{so}(5,5)$, the commutator $[X, Y] = XY - YX$ is also in $\mathfrak{so}(5,5)$.
\end{theorem}
\textit{Proof (Lean 4 Verified):} By expanding the matrix transpose over subtraction and multiplication, and recursively factoring the negative symmetries through the metric tensor $\eta$, the resulting symmetric identity $A - B + B - A = 0$ is resolved using Abelian group closures.

\section{The Split Octonion Spin Factor Embedding}
The expert states are characterized by the $2 \times 2$ Hermitian matrices over split-octonions $H_2(\mathbb{O}')$. A state $X \in H_2(\mathbb{O}')$ takes the form:
\begin{equation}
X = \begin{pmatrix} \alpha & Z \\ \bar{Z} & \beta \end{pmatrix}, \quad \alpha, \beta \in \mathbb{R}, \; Z \in \mathbb{O}'
\end{equation}
Its quadratic form is given by the determinant $\det(X) = \alpha\beta - |Z|^2$, which acts as the Radon-Nikodym information Lagrangian for the expert distribution.

\begin{theorem}[Routing Symmetry Theorem]
There exists an isometric vector embedding $\Phi: H_2(\mathbb{O}') \to \mathbb{R}^{10}$ such that the expert information Lagrangian is exactly the $10D$ quadratic form:
\begin{equation}
\Phi(X)^T \eta \Phi(X) = \det(X)
\end{equation}
Furthermore, for any gauge transformation $G \in O(5,5)$, the transformed expert state preserves this Lagrangian:
\begin{equation}
(G^T \Phi(X))^T \eta (G^T \Phi(X)) = \det(X)
\end{equation}
\end{theorem}
This confirms that the Metriplectic Symplectic Flows generated by $O(5,5)$ act as true isometries on the KAN expert manifold, preserving all local information distances.

\section{Conclusion}
The integration of ArangoDB (ASTAQLHASH) graph tracking and Lean 4 automated theorem proving establishes a rigorous, hallucination-free foundation for Super-K\"ahler geometry in deep neural network optimization.

\section{Computational Implementation and Empirical Validation}

To demonstrate that the $O(5,5)$ gauge-covariant KAN is practically computable, we implemented the proposed $H_2(\mathbb{O}')$ spin factor layer in PyTorch. To guarantee that the network strictly optimizes along the gauge orbits of $O(5,5)$, we parameterize the learnable weights via the Lie algebra $\mathfrak{so}(5,5)$. 

For a free weight matrix $W \in \mathbb{R}^{10 \times 10}$, the Lie algebra projection is computed as:
\begin{equation}
    A = W - \eta_{(5,5)} W^T \eta_{(5,5)}
\end{equation}
which strictly satisfies the orthogonality condition $A \eta + \eta A^T = 0$. The gauge transformation applied to the expert states is then the matrix exponential $G = \exp(A) \in O(5,5)$.

During a 50-step optimization process targeting a non-linear objective (Task Loss), the network demonstrated robust learning while conserving the internal symmetry up to the hardware \texttt{float32} limit ($\sim 10^{-7}$):

\begin{verbatim}
Starting O(5,5) KAN Layer Optimization...
Step  | Task Loss    | Max Entropy Diff     | Symmetry Error ||G*eta*G^T - eta||
---------------------------------------------------------------------------
0     | 5.118130     | 2.3842e-06           | 3.5763e-07
20    | 4.046471     | 2.8610e-06           | 3.7603e-07
49    | 3.523486     | 6.9141e-06           | 4.4486e-07
\end{verbatim}

The empirical results perfectly mirror our Lean 4 kernel-verified theorems: the neural network successfully optimizes its internal state while strictly preserving the isometric metric $G\eta G^T = \eta$ and perfectly conserving the Radon-Nikodym routing entropy.

\end{document}
