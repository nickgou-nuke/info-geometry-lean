{"file":"/home/goutev/repos/info-geometry-lean/lean/InfoGeometry/Singular/MoorePenrose.lean","time":1778532678703,"baseTime":1778532677766,"changes":[{"range":{"start":{"line":262,"character":0},"end":{"line":269,"character":0}},"text":"    have hAB : A.comp B = R.subtypeL.comp (Submodule.orthogonalProjection R) := by\n      ext y\n      simp only [B, moorePenroseInverse, ContinuousLinearMap.comp_apply, Submodule.coe_subtypeL]\n      let y_proj := (Submodule.orthogonalProjection R) y\n      let x_perp := e.symm y_proj\n      have he_x : e x_perp = y_proj := e.apply_symm_apply y_proj\n      have h_Axp : A x_perp = y_proj.1 := by\n        have := Subtype.ext_iff.mp he_x\n        simp [e, mpEquiv, mpRestricted] at this\n        exact this\n      exact h_Axp\n    rw [hAB]\n    exact IsSelfAdjoint.star_eq (Submodule.isSelfAdjoint_orthogonalProjection R)\n  · -- star (BA) = BA\n    have hBA : B.comp A = Kp.subtypeL.comp (Submodule.orthogonalProjection Kp) := by\n      ext x\n      simp only [B, moorePenroseInverse, ContinuousLinearMap.comp_apply, Submodule.coe_subtypeL]\n      have hAx : A x ∈ R := LinearMap.mem_range.mpr ⟨x, rfl⟩\n      have hproj : (Submodule.orthogonalProjection R) (A x) = ⟨A x, hAx⟩ := by\n        exact Submodule.orthogonalProjection_mem_subspace_eq_self ⟨A x, hAx⟩\n      rw [hproj]\n      have h_split : x = (Submodule.orthogonalProjection (A.ker)) x + (Submodule.orthogonalProjection Kp) x := by\n        exact (Submodule.orthogonalProjection_add_orthogonalProjection_orthogonal (A.ker) x).symm\n      rw [h_split, map_add]\n      have h_ker : A (Submodule.orthogonalProjection (A.ker) x) = 0 := by\n        simp only [Submodule.coe_orthogonalProjection, LinearMap.mem_ker.mp (Submodule.coe_mem _)]\n      rw [h_ker, zero_add]\n      let x_perp := (Submodule.orthogonalProjection Kp) x\n      have he_val2 : e x_perp = ⟨A x, hAx⟩ := by\n        ext; simp [e, mpEquiv, mpRestricted]; rw [h_split, map_add, h_ker, zero_add]\n      rw [← he_val2, ContinuousLinearEquiv.symm_apply_apply]\n      rfl\n    rw [hBA]\n    exact IsSelfAdjoint.star_eq (Submodule.isSelfAdjoint_orthogonalProjection Kp)\n\n","rangeOffset":10751,"rangeLength":211}]}