import InfoGeometry.Krein.DoubledSpace
import InfoGeometry.Canonical.BogoliubovRGFlowBridge
import InfoGeometry.Canonical.TensorTowerColimit

/-!
# Retired scalar Hestenes/Krein colimit-flow surface

The former owner represented the doubled carrier by `ℝ × ℝ` and prescribed a
coordinate vector field.  Its staged map was not a categorical colimit map and
its Lyapunov calculation was not an operatorial metriplectic evolution.

The canonical carriers and transport owners are `Krein.DoubledSpace`,
`BogoliubovRGFlowBridge`, and `TensorTowerColimit`.  Any future invariant or
contraction theorem must be stated for those objects with an explicit
continuous-linear/operator flow and compatibility proof.  This module exports
no scalar critical-locus, attractor, or colimit theorem.
-/

namespace InfoGeometry.SuperMetriplectic.HestenesKrein

end InfoGeometry.SuperMetriplectic.HestenesKrein
