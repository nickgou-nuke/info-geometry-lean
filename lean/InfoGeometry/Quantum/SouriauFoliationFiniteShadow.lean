import InfoGeometry.OperatorAlgebra.ModularWeightTrace
import InfoGeometry.OperatorAlgebra.ModularSignCPT

/-!
# Retired finite Souriau/Tomita shadow

The former contents of this module were a standalone diagonal `2 × 2` matrix
model.  Its declarations were not consumed by any maintained theorem and did
not construct a noncommutative algebra, a modular weight, a standard form, or
an anti-linear Tomita conjugation.

The maintained noncommutative owners are now:

* `InfoGeometry.OperatorAlgebra.ModularWeightTrace` for modular weights and
  KMS covariance;
* `InfoGeometry.OperatorAlgebra.ModularSignCPT` for operatorial modular/CPT
  flow laws.

This compatibility file intentionally exports no scalar replacement theorem.
-/

namespace InfoGeometry.Quantum.SouriauFoliationFiniteShadow

end InfoGeometry.Quantum.SouriauFoliationFiniteShadow
