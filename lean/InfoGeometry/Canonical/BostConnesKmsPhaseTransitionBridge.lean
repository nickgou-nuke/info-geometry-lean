import InfoGeometry.Algebra.BostConnesKMSPhaseTransition
import InfoGeometry.Algebra.FiniteSpinAlgebra
import InfoGeometry.Canonical.BostConnesKMS
import InfoGeometry.Canonical.BostConnesKMS1ColimitBridge
import InfoGeometry.Arithmetic.BostConnesNativeZetaPartition

/-!
# Retired abstract Bost--Connes KMS facade

The former owner bundled positivity, monotonicity, an exponential weight
identity, and a default `residue_at_one` field into an abstract record.  It
did not construct a C*-dynamical system, a KMS state, a Dirichlet series, or a
pole limit.  The maintained algebraic KMS phase-transition, native partition,
and colimit owners are imported above.

No abstract scalar `BostConnesKmsDatum` or declared residue API is exported.
-/

namespace InfoGeometry.Canonical.BostConnes

end InfoGeometry.Canonical.BostConnes
