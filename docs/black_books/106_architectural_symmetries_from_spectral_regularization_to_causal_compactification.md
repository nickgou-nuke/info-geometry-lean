# Architectural Symmetries in Mathematical Physics
## Isomorphic Translation Charter: Complex Modular Theory -> Doubled Real Krein/Hestenes

## Non-negotiable mandate

This chapter is not an analogy note. It is a translation mandate.

Goal:

- replace external complex unit `i` by internal Hestenes phase axis `K = J o eps`,
- reformulate the modular Hamiltonian and modular automorphism theory in the
  doubled real Krein representation,
- treat the complex and doubled-real formulations as isomorphic representations
  of one underlying structure.

## 1. Representation map

Let `H_C` be the complex representation and `H_2 := DoubledSpace E` the
repo-native real doubled representation.

Internal axes on `H_2`:

- `J` : modular involution lane,
- `eps` : spectral sign involution lane,
- `K := J o eps` : internal phase axis with `K^2 = -1`.

Replacement rule:

`i` in complex formulas is represented by internal operator `K` on `H_2`.

## 2. Operator-level isomorphic translation

For each complex-linear operator `A` in the complex modular lane, define a
translated real-doubled operator `T(A)` on `H_2`.

Required transport laws:

1. `T(A B) = T(A) T(B)`
2. `T(A^*) = T(A)^*_K`
3. `T(i A) = K T(A)`
4. `T(exp(i X)) = exp(K T(X))` on admissible domains

This is the core algebraic isomorphism requirement.

## 3. True modular Hamiltonian in doubled real language

Complex modular lane:

- modular operator `Delta`,
- modular Hamiltonian `H_mod = -log Delta`,
- modular unitary flow `U_t = Delta^(i t) = exp(-i t H_mod)`.

Doubled real Krein/Hestenes lane:

- translated modular operator `Delta_K := T(Delta)`,
- translated modular Hamiltonian
  `H_mod_K := -Log_support(Delta_K)`,
- translated modular flow
  `U_t_K := Delta_K^(K t) = exp(-t K H_mod_K)`.

This is the explicit formulation target.

`H_mod_K` is the true modular Hamiltonian in the doubled real representation.

## 4. Modular automorphism theory in doubled real language

Complex formula:

`sigma_t^phi(A) = Delta_phi^(i t) A Delta_phi^(-i t)`.

Doubled real translation:

`sigma_t^{phi,K}(X) = Delta_{phi,K}^(K t) X Delta_{phi,K}^(-K t)`

equivalently

`sigma_t^{phi,K}(X) = U_t_K X U_{-t,K}`.

Transport requirement:

`T(sigma_t^phi(A)) = sigma_t^{T(phi),K}(T(A))`.

This is the exact automorphism translation clause, not a heuristic.

## 5. Relative modular operator and cocycle translation

Complex relative lane:

- `Delta_{psi|phi}`,
- relative Hamiltonian `H_{psi|phi} = -log Delta_{psi|phi}`,
- cocycle `(D psi : D phi)_t`.

Doubled real lane:

- `Delta_{psi|phi,K} := T(Delta_{psi|phi})`,
- `H_{psi|phi,K} := -Log_support(Delta_{psi|phi,K})`,
- cocycle in K-language:
  `(D psi : D phi)_{t,K}` generated by `K`-modular flow.

Transport targets:

1. `T(H_{psi|phi}) = H_{psi|phi,K}`
2. `T((D psi : D phi)_t) = (D T(psi) : D T(phi))_{t,K}`
3. chain rule preserved under `T`.

## 6. Support and projector package integration

Canonical support in modular theory is spectral support of `Delta`.

Repo executable lane uses projector package (`Preg`, `Pzero`, Drazin/MP)
to realize support operationally on doubled carriers.

Hard requirement for closure:

- prove when executable support equals translated canonical support,
- then prove `H_reg = -log(Delta|Preg)` realizes `H_mod_K` on the regular lane.

So projector package is not a substitute for modular theory.
It is the executable realization layer for the same object.

## 7. Lean theorem targets (explicit)

```lean
theorem i_to_K_transport :
  T (Complex.I • A) = Kcomp (T A)

theorem modular_hamiltonian_transport :
  T (-log_on_support Delta) = -log_on_support (T Delta)

theorem modular_flow_transport :
  T (Delta ^ (Complex.I * t)) = (T Delta) ^ (K * t)

theorem modular_automorphism_transport :
  T (sigma_complex phi t A)
    = sigma_krein (T phi) t (T A)

theorem relative_cocycle_transport :
  T (connesCocycle psi phi t)
    = connesCocycleK (T psi) (T phi) t
```

These are the theorem surfaces that make the translation exact.

## 8. Representation-equality clause

This repository commits to the following claim schema:

- complex modular theory,
- doubled real Krein/Hestenes modular theory,
- twistor/conformal compactification lane

are equal-validity representation surfaces of one structure, whenever
translation morphisms and preservation theorems are present.

No prose equivalence is accepted without typed transport and closure proofs.

## 9. Final statement

The target is not "compare" or "inspire".
The target is:

`modular automorphism theory rewritten isomorphically in doubled real Krein/Hestenes language`,

with

`true modular Hamiltonian explicitly represented as H_mod_K = -Log_support(Delta_K)`

and

`i replaced by internal K at the level of definitions, flow, and cocycle laws`.
