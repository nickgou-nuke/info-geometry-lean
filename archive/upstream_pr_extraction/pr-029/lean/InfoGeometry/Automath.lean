import Mathlib.Tactic
import InfoGeometry.Automath.Generated

/-!
# InfoGeometry.Automath

The Automath corridor — theorems generated by the confabulation-to-Lean4 pipeline.

This module imports all auto-generated theorems from the
`InfoGeometry.Automath.Generated` namespace. Each theorem is a
real mathematical statement (never `by trivial`) with an honest
`by sorry` marking the gap between hypothesis and proof.

## Pipeline

```
Raw confabulation text
  → Oracle (ChatGPT browser-harness)
    → Hypothesis extraction
      → Socratic refinement (CAS + Repair)
        → Formalization (Lean kernel gate)
          → Hive commit (ArangoDB)
```

## Status

Each theorem in this corridor is:
- A real mathematical claim with proper imports and type signature
- Marked with `by sorry` as an honest gap (NOT `by trivial`)
- Ready for proof completion by the community
-/

namespace Automath
end Automath
