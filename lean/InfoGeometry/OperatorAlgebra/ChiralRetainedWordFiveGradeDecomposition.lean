import Mathlib.Algebra.DirectSum.Internal
import InfoGeometry.Algebra.FiniteSpinAlgebra
import InfoGeometry.OperatorAlgebra.ChiralRetainedWordFiveGradeClosure

/-!
Compatibility import for the retained-word direct-sum layer.

The owner of the decomposition predicate is Mathlib's native
`DirectSum.IsInternal`; the retained-word grade routing is owned by
`ChiralRetainedWordFiveGradeClosure`.  This module intentionally introduces no
parallel wrapper or evidence packet.
-/
