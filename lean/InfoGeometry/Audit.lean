import InfoGeometry.Foundations.AxiomaticDependencyGraph

/-!
# Audit

Compatibility import for the canonical Hodge-Krein dependency graph.

The theorem owner is `InfoGeometry.Foundations.AxiomaticDependencyGraph`
(`namespace Audit`).  This file intentionally declares no duplicate constants:
it keeps the historical module path build-reachable without shadowing the
canonical owner.
-/
