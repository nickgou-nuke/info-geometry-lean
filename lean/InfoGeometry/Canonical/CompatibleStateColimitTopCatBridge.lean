import InfoGeometry.Canonical.FilteredStarInductiveCoconeTopCat
import InfoGeometry.Algebra.FiniteSpinAlgebra

/-!
# Compatible state readouts through the TopCat colimit

The native owner is
`CStarStateColimit.Native.ContinuousStarInductiveSystem.StarInductiveCocone`.
It constructs the `TopCat` state cocone and proves its colimit inclusion law
for arbitrary noncommutative star-algebra stages.  This export contains no
matrix-specific or scalar readout packet.
-/
