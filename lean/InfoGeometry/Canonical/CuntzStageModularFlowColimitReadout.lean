import InfoGeometry.Canonical.BoundedModularKMSBridge
import InfoGeometry.Algebra.FiniteSpinAlgebra

/-!
# Modular-flow readout export

The genuine owner is `BoundedModularKMSBridge`: it formalizes the inner
operator flow, its group laws, and invariance of the native KMS state on an
arbitrary noncommutative algebra.  No matrix trace or stage-specific scalar
readout is introduced here.
-/
