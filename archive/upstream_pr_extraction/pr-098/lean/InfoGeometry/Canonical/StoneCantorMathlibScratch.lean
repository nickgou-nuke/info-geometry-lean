import InfoGeometry.Canonical.StoneCantorMathlibPR

/-!
# Stone/Cantor mathlib extraction scratch

Compatibility module for older imports.  The theorem-owning PR surface has been
factored into `StoneCantorMathlibPR`, which in turn depends only on
`PiCylinderMathlib` and `CantorCylinderTopology`.

This file intentionally owns no definitions or theorems.
-/
