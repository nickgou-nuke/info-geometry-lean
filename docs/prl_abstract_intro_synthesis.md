# Publication Note Status

This file is no longer a live abstract or introduction draft.
It is a placeholder reminding readers that publication-facing text must be regenerated from the current codebase rather than copied from older synthesis prose.

## If you need current material

Start from the owner modules instead:
- projective / relative-potential root: `ProjectiveStateCore`, `RelativeGeneratorCore`, `RedLine`
- KMS and operator transport: `SinkhornKMSCore`, `KMSSinkhorn*`, `TomitaTakesaki`
- geometry trunk: `RicciMongeAmpere`, `CalabiYau*`, `PerelmanW`
- singular / conformal packet: `Drazin`, `MoorePenrose`, `ConformalAlgebra`, `SingularBoundaryCorrection`

Then extract current theorem names directly from code and maintained reports.
