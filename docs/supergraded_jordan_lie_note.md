# Supergraded Jordan–Lie Emergence Note

This note captures the recent synthesis in operatorial form.

## Core claim

On the doubled real Krein carrier, **metric** and **curvature** are emergent.
They arise from the **Jordan–Lie split** of the Clifford product, which is
canonically selected by the supergrading (Fock parity) and the Hestenes
phase grading (`K = Jε`).

## Algebraic split

For doubled-space endomorphisms `A, B`:

- **Jordan product** (symmetric):
  ```
  A ∘ B = 1/2 · (A ∘ₗ B + B ∘ₗ A)
  ```
- **Lie product** (antisymmetric):
  ```
  [A, B] = 1/2 · (A ∘ₗ B - B ∘ₗ A)
  ```

Here `∘ₗ` is the operator composition on the doubled carrier.

## Supergrading selects the channel

With the canonical supergraded bracket carried by
`SuperHestenesKaehlerDatum`:

- **Even–even** channel yields the Lie product (commutator).
- **Odd–odd** channel yields the Jordan product (anticommutator).

So the **Jordan sector** is the metric/variance channel,
and the **Lie sector** is the Berry/curvature channel.

## Operatorial, noncommutative stance

No diagonalization or scalar proxy is used. All statements are made at the
level of noncommuting operators on `DoubledSpace E`, with the Clifford
structure fixed by the Hestenes axes `J`, `ε`, `K`.

## State-dependent evolution (non-spacetime view)

Evolution is **state-dependent**, not space-dependent. There is no primitive
spacetime here; spacetime is a secondary chart extracted from families of
evolution parameters.

The propagator is generated by operatorial data (relative modular operators),
and the **distance** is an entropic cost derived from the difference of the
generating potential after lifting to the operator level. In this view:

- The exponential map is the **propagator** of informational evolution.
- Cartan generators give the canonical boost directions in state space.
- Any apparent spacetime coordinates are emergent from the transport data.

This aligns with the doubled Krein structure and the algebra/commutant pairing:
stabilization can arise from the interaction between the algebra and its
commutant (two-sheet dynamics), not from an external geometric background.

## Lean formalization

The basic operatorial split is formalized in:

- `lean/InfoGeometry/Canonical/SuperJordanLie.lean`

Key lemmas:

- `fockCommutator_eq_two_smul_lieProduct`
- `fockAnticommutator_eq_two_smul_jordanProduct`

These explicitly tie the supergraded brackets to the Jordan–Lie split, making
the emergent metric/curvature claim a verified statement in Lean.
