# Closure Debt Ledger

> Status: `reference memory`
> Audited: 2026-05-02
> Note: Re-audit against current code before using for policy, design claims, or status.
> See: [README.md](../README.md), [docs/README.md](README.md), [docs/CODEBASE_STATUS.md](CODEBASE_STATUS.md)

This ledger is stale unless re-audited against the live Lean source. Do not use
it as current proof authority.

Last updated: 2026-04-22 (Sofia/Interactive)

This ledger tracks closure debt as executable proof obligations on the
operator-algebraic spine. It is not an essay surface; each row maps to Lean
anchors and CI gates.

## Mission Loop Contract

The repo-local mission loop treats this ledger as the authoritative backlog.
On each heartbeat it should:

1. select the smallest payable debt row,
2. patch exactly one native Lean surface,
3. run the narrowest build target for that surface,
4. escalate only to the frontier gate or `strict-check.sh` when the touched
   target crosses a broader cluster boundary,
5. record the result back into the ledger or an explicit open-problem report.

Open problem sockets stay open until kernel-checked Lean or imported mathlib
theorems discharge them. Literature ownership is only advisory until formalized.

## Debt Register

| Debt ID | Requirement | Current Formal Anchors | Missing Derivation Target | Owner Surface | Status |
|---|---|---|---|---|---|
| D1 | Prove commutator forcing (`[K, I]`) from symmetry/Cartan assumptions, not by direct commutation assumption. | `winding_orbit_periodicity` in `WindingOrbitClosure`; forcing owner `HasClockAxisForcingSeed`, `modularTransportGenerator_commutes_clockAxis_of_forcingSeed`, and `winding_orbit_periodicity_of_forcingSeed`; `modularAnomalyGenerator_eq_commutator_shadow` and `unified_anomaly_bridge` in `ModularAnomaly`; odd-sector forcing lemmas `commutator_IK_mem_odd` / `commutator_KI_mem_odd`; real grade-intersection theorem `eq_zero_of_mem_even_and_odd`; dual-grade zero theorem `commutator_KI_eq_zero_of_dual_grade_forcing`; concrete phase-axis Cartan owner `phaseAxisSymmetricLieAlgebra`, `modularGeneratorGaugePart_mem_phaseAxis_even`, `modularGeneratorScalePart_mem_phaseAxis_odd`, `scaleClockCartanForcingData`, `scaleClock_commutator_mem_phaseAxis_odd`; winding bridges `modularTransportGenerator_commutator_clockAxis_eq_zero_of_cartanDualGrade`, `modularTransportGenerator_commutes_clockAxis_of_cartanDualGrade`, and `winding_orbit_periodicity_of_cartanDualGrade`; scale-source law `modularTransportGenerator_clockAxis_commutator_eq_cartanScaleSource`; faithfulness anchors `IsClockFaithfulExponentialBranch`, `LocalClockGaugeSymmetryCertificate`, `clockEquilibrium_iff_windingOrbitObstruction_eq_zero_of_clockFaithfulBranch`. | The zero and periodicity steps are now Cartan-backed. Commutation `[K, I] = 0` is derived from Cartan-grade symmetry (`scalePart_eq_zero_iff_mem_even`). The reverse direction (zero branch obstruction forces commutation) is now formally gated by the `IsClockFaithfulExponentialBranch` hypothesis, which can be derived from a `LocalClockGaugeSymmetryCertificate`. | `lean/InfoGeometry/Core/CartanPhaseAxisForcing.lean`, `lean/InfoGeometry/Canonical/PhaseAxisCartanSymmetricLie.lean`, `lean/InfoGeometry/Canonical/WindingOrbitClosure.lean`, `lean/InfoGeometry/Quantum/ModularAnomaly.lean`. | Closed (Equilibrium) / Refined (Reverse) |
| D2 | Replace Drazin/Weyl placeholders with constructive spectral proofs. | `IsWeylCompatible` equivalences in `TriadicWeylBridge`; constructive owner shim `drazinInverse_isWeylCompatible`; local-symmetry forcing route `spectralEpsilon_conj_constructiveDrazinCandidate_eq`, `constructiveDrazinCandidate_commutes_spectralEpsilon_of_localWeylSymmetry`, and `constructiveRieszWeylData_of_localWeylSymmetry` in `DrazinWeylConstructive`. | The raw candidate-commutation package can now be constructed from an ε-symmetric constructive Riesz problem plus regular-inverse uniqueness. Remaining work is downstream replacement of any direct `ConstructiveRieszWeylData` assumptions with the local-symmetry constructor where source data is available. | `lean/InfoGeometry/Canonical/DrazinWeylConstructive.lean`, `lean/InfoGeometry/Quantum/TriadicWeylBridge.lean`, `lean/InfoGeometry/Canonical/DrazinInfiniteCore.lean`. | Reduced |
| D3 | Prove Sinkhorn as defect-reduction flow (primary relative-volume monotonicity, derived thermodynamic router readout). | Relative-volume owner surface `δ_relVol`, `δ_relVol_nonneg`, `δ_relVol_next_le`; scalar work/heat bookkeeping `availableWorkRN`, `dissipatedHeatRN`, `availableWorkRN_nonneg`, `trajectoryRNBarrierNext_add_availableWorkRN_eq_δ_relVol`; thermodynamic readout lane `RouterDefectThermodynamicBridge`, `δ_odd_thermo`, `δ_ZD_thermo`, `δ_odd_thermo_le_ZD`; RN budget lane `SinkhornRNBarrierThermodynamicComparison`, `SinkhornRNBarrierThermodynamicComparison.ofResidualReadoutEqBarrier`, `δ_odd_thermo_le_ZD_of_rnBarrierComparison`, `δ_odd_thermo_next_le_ZD_of_relativeDefectBridge`; positive-count/profile lane `SinkhornRNBarrierProfileLift`, `PhaseRNBarrierProfileLift`, `RowRNBarrierCountProfileLift`, `ColRNBarrierCountProfileLift`, `RowRNBarrierCountProfileLift.ofMassNormalized`, `ColRNBarrierCountProfileLift.ofMassNormalized`, `PhaseRNBarrierProfileLift.ofRowCounts`, `PhaseRNBarrierProfileLift.ofColCounts`; profile packet lane `WeylThermodynamicProfileComparison`, `WeylThermodynamicProfileComparison.ofSinkhornRNBarrier`, `operatorInformationNormReadout_le_of_sinkhornRNBarrierProfile`; finite gauge-thermodynamics lane `rowNormalization_is_leftWeylGauge`, `colNormalization_is_rightWeylGauge`, `sinkhornTwoStep_is_twoSidedWeylGauge`, `sinkhornStep_RNForce_monotone`, `sinkhornGaugeBudget_controls_approxKMS`, `sinkhornGaugeBudget_zero_forces_KMSClosure`. | The row/column RN barrier equality is now derived for positive observed count profiles whose total carrier mass is `n`, and the finite Sinkhorn step is formally packaged as diagonal Weyl-gauge thermodynamics with RN force/budget and KMS budget consumption. The RN-barrier thermodynamic comparison no longer needs an extra central-budget hypothesis once the residual readout is identified with the current barrier: `SinkhornRNBarrierThermodynamicComparison.ofResidualReadoutEqBarrier` derives that budget from `δ_odd_thermo_le_ZD`. Remaining work is trajectory plumbing: show each Sinkhorn phase supplies the appropriate mass-normalized row/column count profile, or carry that mass-normalization certificate explicitly. The active lane is scaled `informationGeometricRelativeNorm`, not RMS `relativeInformationNorm`, and not the legacy ambient operator norm. | `lean/InfoGeometry/LLM/SinkhornDefectFlow.lean`, `lean/InfoGeometry/LLM/TrialityMoE.lean`, `lean/InfoGeometry/Canonical/SinkhornFoundation.lean`, `lean/InfoGeometry/Canonical/SinkhornGaugeThermodynamicsBridge.lean`, `lean/InfoGeometry/Canonical/RelativePotentialCountBridge.lean`, `lean/InfoGeometry/Canonical/RelativePotentialCore.lean`. | Reduced; theorem-backed RN-budget constructor landed |
| D4 | Hard CI gates: no new `sorry`, no new `axiom`, strict build on closure trunks. | `check_closure_debt_gate.py` scans all closure frontier modules for banned tokens and required anchors; `strict-check.sh` invokes that gate and strict-builds the closure-debt target trunks. | Closed as process debt. Ongoing obligation is maintenance: any D1-D3 change must keep the gate green. | `tools/quality/check_closure_debt_gate.py`, `tools/quality/closure_debt_gate.json`, `scripts/quality/strict-check.sh`. | Closed / enforced |
| D5 | Package the finite grand-canonical chemical potential and its Fock number-operator analogue without claiming an equality between finite count profiles and Fock operators. | Finite lane: `chemicalPotentialGauge`, `shiftedEnergy_eq_energy_sub_chemicalPotentialGauge`, `chemicalPotentialGauge_add`, `grandCanonicalMassieuPotential`, `chemicalPotential_conjugate_count_readout`, `inverseTemperature_conjugate_shiftedEnergy_readout`. Fock lane: `fockNumberGauge`, `fockNumberGauge_add`, `grandCanonicalFockGenerator_eq_hamiltonian_sub_fockNumberGauge`, `fockNumberGauge_is_mu_times_numberOperator`, `grandCanonicalFockGenerator_zero_mu`. | Closed for same-form finite/Fock coupling: finite `μ` couples to count observable `N`, Fock `μ` couples to `bogoliubovNumberOperator`, and both are affine `base - μN` grand-canonical forms. Remaining future work is a representation theorem relating a finite count profile to a Fock occupation readout, if such a theorem is needed. | `lean/InfoGeometry/GrandCanonical/Core.lean`, `lean/InfoGeometry/Canonical/GrandCanonicalGaugePotentialBridge.lean`, `lean/InfoGeometry/Canonical/BogoliubovFockSuper.lean`, `lean/InfoGeometry/Canonical/GrandCanonicalFockNumberBridge.lean`. | Closed for same-form finite/Fock number coupling |
| D6 | Reconcile the chiral light-cone operator algebra with the KKT/TKK chiral closure and the Fock occupation lane without collapsing distinct charge notions. | `krein_signed_polarization_readout` (`ε = x_+ - x_-`), `drazin_signed_chiral_polarization` (`Γ_G = P_R - P_L`), `drazin_supercharge_eq_chiral_polarization_defect` (`Q_D = [P_D, Γ_G]`), `drazin_chiral_defect_eq_right_minus_left`, primitive CAR/CCR closure, `fock_occupation_eq_creation_after_annihilation`, and `ChiralLightconeKKTClosure.ofOwners`. | Closed as a reconciliation surface: the same signed-polarization/commutator pattern is source-backed across light-cone, KKT/TKK, Drazin supercharge, and Fock occupation coupling. Remaining future work is a representation theorem if one wants to identify a Fock number readout with a chiral defect, a central charge, or a finite count profile. | `lean/InfoGeometry/Canonical/OperatorSpacetimeObservables.lean`, `lean/InfoGeometry/Canonical/GlobalChiralDecomposition.lean`, `lean/InfoGeometry/Canonical/ClosureDrazinBridge.lean`, `lean/InfoGeometry/Canonical/SuperchargeCARCCRBridge.lean`, `lean/InfoGeometry/Canonical/BogoliubovFockSuper.lean`, `lean/InfoGeometry/Canonical/ChiralChargeFockNumberBridge.lean`. | Closed for reconciliation; representation theorem still future work |
|| D7 | Establish the formal derivation of the 4x4 Dirac-Souriau operator as the reduced owner surface of the $C\ell(4,4)$ informational gravity sector. | `DiracSouriauSector`, `HasDrazinInverse`, `souriauEntropy`, `IsBPSProtected`. | Prove the existence of the 4x4 Drazin inverse from the block-decoupling properties (Hypothesis 1) and derive the `souriauEntropy` exactness from the underlying $C\ell(4,4)$ Pfaffian/Berezinian duality. | `lean/InfoGeometry/Canonical/DiracSouriauOperator.lean`. | Open |
|| D8 | Pay the full coadjoint-orbit Souriau Hessian burden without hiding analytic inverse-function assumptions. | Infinite theorem surface: `InfiniteCoadjointOrbitHessianContext`, `full_infinite_dimensional_coadjoint_orbit_hessian_theorem`, `full_infinite_dimensional_coadjoint_orbit_hessian_metriplectic_theorem`. Smooth inverse owner: `LegendreHessianInverseContext`, `entropy_hessian_eq_fisher_inverse`, `legendre_hessian_inverse_packet`. Discharge bridge: `SmoothLegendreInverseWitness`, `legendre_entropy_hessian_eq_inverse_fisher_at`, `souriau_inverse_fisher_from_smooth_legendre_packet`. | The inverse-Hessian leg is connected to the smooth Legendre two-sided inverse owner under explicit readout-matching fields. Remaining analytic debt is the concrete infinite coadjoint-orbit construction: prove partition differentiability, covariance identity, strict Fisher positivity, and the readout-matching fields from a specific smooth orbit/model rather than supplying them as context data. | `lean/InfoGeometry/Canonical/SouriauCoadjointOrbitMetriplecticTheorem.lean`, `lean/InfoGeometry/Geometry/LegendreHessianInverse.lean`, `lean/InfoGeometry/Canonical/SouriauThermodynamics.lean`, `lean/InfoGeometry/Canonical/SouriauLieThermoKKTBridge.lean`. | Open / reduced |
|| D9 | Keep multilingual/literature Souriau theorem translation proof-carrying and prevent KKT/TKK/weight/metriplectic/super-fermion drift. | Translator anchors `claimA_massieu_eq_log_partition`, `claimB_firstDerivatives_eq_moments`, `claimC_hessian_eq_fisher_eq_covariance`, `claimD_fenchelLegendre_contact_packet`, `claimE_entropyProduction_nonneg_of_PSD`, KKT anchors `claimK_kktEntropyStationarity_packet`, `structuredSouriauKKTTranslatorPacket`, representation-weight anchor `claimW_representationWeight_lifts_to_densityTransport`, super-coadjoint readout anchor `claimS_superCoadjoint_stress_supercurrent_readouts`, super-fermion anchor `claimF_superSouriauFermionGas_packet`, infinite super-coadjoint anchor `claimG_infiniteSuperCoadjointMetriplectic_packet`, TKK/Jordan-Lie anchor `claimT_splitCl44_TKK_JordanLie_packet`, metriplectic split anchor `claimM_coadjointLeaf_Casimir_transverseOnsager_packet`, and infinite enrichment `claimCDE_fullCoadjointOrbit_hessian_metriplectic_packet`. | Literature/prose is normalized into theorem packets while Karush-Kuhn-Tucker remains an explicit optimization hypothesis packet, representation weights are lifted only through the density-weight owner, supergraded fermion-gas claims are projected only through the SuperSouriau/Fock/CAR/Weyl-supertrace owners, the infinite super lift is projected only through `FullCoadjointOrbitMetriplecticContext` with explicit orbit/invariance/Weyl/supertrace hypotheses, TKK/Jordan-Lie is projected only from the explicit split `Cl(4,4)` closure packet, and symplectic-leaf/transverse dissipation is projected only from the full coadjoint metriplectic context. Remaining work is source expansion and concrete model instantiation, not theorem-packet proliferation. | `lean/InfoGeometry/Canonical/SouriauTheoremTranslatorPacket.lean`, `lean/InfoGeometry/Canonical/SouriauLieThermoKKTBridge.lean`, `lean/InfoGeometry/Canonical/SuperSouriauFermionGasBridge.lean`, `lean/Info... [truncated]
|| D10 | Replace bare conformal Fisher/Onsager self-response positivity with proof-backed operatorial positivity statements. | `ConformalFisherOnsagerPositiveContext` still carries the old compatibility interface for downstream users; `ConformalSquareFisherOnsagerPositiveContext`, `selfResponse_nonneg`, `toPositiveContext`, and `fisherOnsagerProduction_nonneg_of_squareResponse` derive the nonnegativity field from a real-square witness on the conformal doubled-carrier operator response. | The selected conformal self-response no longer needs a bare `selfResponse_nonneg` when a square-response witness is available. Remaining work is to construct the positivity from concrete conformal/Weyl/KKT owner theorems, or replace downstream users with the square-response context where the amplitude is known. | `lean/InfoGeometry/Canonical/SouriauConformalKKTContext.lean`, `lean/InfoGeometry/Canonical/OnsagerReciprocity.lean`. | Open / reduced |
|| D11 | Replace bare conformal operator-partition positivity inside KKT admissibility with proof-backed operatorial witnesses. | `ConformalPositivePartitionWitness`, `operatorPartition_pos`, `ConformalOperatorAdmissibilityWitness`, `operatorAdmissible`, and `toClosureContext` derive the old `hOperatorAdmissible` field from explicit cone membership plus a positive-floor-plus-square witness for the noncommutative operator partition. | Conformal closure contexts no longer need to be assembled from a raw `0 < operatorPartition` proof when a positive partition witness is available. Remaining work is the stronger Cartan/self-dual-positive-cone constructor that would supply such witnesses from symmetric-space positivity rather than explicit floor/square data. | `lean/InfoGeometry/Canonical/SouriauConformalKKTContext.lean`. | Open / reduced |
|| D12 | Discharge the Fenchel-Legendre uniqueness and macroscopic thermodynamic trace-coupling assumptions for the Navier-Stokes-Legendre bridge. | `fenchelGap_eq_zero_iff_contact`, `trace_madelung_velocity_eq`, `madelung_divergence_free_iff`, `contact_iff_collapsed_trace_zero`, and `fenchel_legendre_gap_zero_iff_madelung_divergence_free` in `NavierStokesLegendre`. | The linear algebra lemmas (`trace_madelung_velocity_eq` and `madelung_divergence_free_iff`) and the Fenchel-Legendre uniqueness theorem (`hUniq`) are fully theorem-backed and proven natively. Remaining open debt is the physical trace-free coupling under the doubled Krein carrier (`hContact`) and the infinite-temperature alignment constraint (`h_beta`). | `lean/InfoGeometry/Capstone/NavierStokesLegendre.lean`. | Open / reduced |
| D13_bracket_preserve | Prove that the colimit injection maps strictly preserve the Lie bracket (commutator). | `ofStage_preserves_commutator` in `CliffordColimitDynamics`. | None — proved via RingHom properties (`map_sub`, `map_mul`). | `lean/InfoGeometry/Canonical/CliffordColimitDynamics.lean`. | **Closed** |
| D14_global_bracket | Prove that any two elements in the transfinite colimit share a common finite-stage representative. | `global_colimit_representatives` in `CliffordColimitDynamics`. | None — proved via `_root_.DirectLimit.exists_eq_mk₂`. | `lean/InfoGeometry/Canonical/CliffordColimitDynamics.lean`. | **Closed** |
|| D15_witten_moebius_zero | Prove that the Pin(5,5) chiral anomaly cancellation survives the transfinite limit. | `witten_moebius_index_zero_preserved` in `CliffordColimitDynamics`. | None — proved via RingHom preservation of multiplication and negation. | `lean/InfoGeometry/Canonical/CliffordColimitDynamics.lean`. | **Closed** |
|| D16_parabolic_time_clock | Formalize Time = Modular Flow = de Rham Monodromy (Connes-Rovelli thermal time hypothesis). | `parabolicTimeClock`, `modularFlow_nilpotent_generator`, `deRhamResidue_nilpotent` in `ModularMonodromyClock`. | None — proved via Derivation structure and nilpotency. SymPy verification in `tools/infra/parabolic_time_monodromy.py`. | `lean/InfoGeometry/Canonical/ModularMonodromyClock.lean`. | **Closed** |


## Gate Contract

1. `G1: No placeholder debt in closure modules`
   Command: `python3 tools/quality/check_closure_debt_gate.py --policy tools/quality/closure_debt_gate.json`
   Fails on any `sorry`, `admit`, `postulate`, or `axiom` token inside the tracked closure modules.

2. `G2: Anchor invariants`
   Same command as `G1`.
   Fails if any required theorem anchor listed in `closure_debt_gate.json` is missing.

3. `G3: Strict build of closure trunks`
   Enforced by `scripts/quality/strict-check.sh` via explicit `lake build` over closure target modules:
   `InfoGeometry.Core.CartanPhaseAxisForcing`, `InfoGeometry.Canonical.WindingOrbitClosure`,
   `InfoGeometry.Quantum.ModularAnomaly`, `InfoGeometry.Canonical.DrazinWeylConstructive`,
   `InfoGeometry.Quantum.TriadicWeylBridge`, `InfoGeometry.LLM.TrialityMoE`, and
   `InfoGeometry.LLM.SinkhornDefectFlow`.

4. `G4: Debt ledger update discipline`
   Any PR touching D1-D3 owner modules must update this file status and targets.

## Immediate Milestones

1. D1 milestone: Cartan-grade forcing now has three source-backed layers: the abstract real grade-intersection theorem (`even ∩ odd = 0`), the concrete phase-axis Cartan owner (`θ(A) = -KAK`, gauge even, scale odd, clock even), and the winding owner’s scale-source law (`[K,I] = 2 • scalePart ∘ I`). An explicit Cartan dual-grade certificate derives both the `Commute` witness and winding periodicity. `IsDetailedEquilibriumSeed` (`scalePart = 0`) remains the closed equilibrium constructor; broader non-equilibrium closure still needs even-membership certificates for the scale-clock commutator, not hidden phase-linearity.
2. D2 milestone: expose constructive Drazin -> Weyl preservation theorem (no nonconstructive fallback).
3. D3 milestone: owner order is explicit and gate-tracked: relative-volume RN barrier (`δ_relVol`) is primary; one-step RN drop is tracked by `availableWorkRN`/`dissipatedHeatRN`; thermodynamic router readout is carried by `δ_odd_thermo`; and Sinkhorn RN data reaches the operator lane through `WeylThermodynamicProfileComparison`. The row/column count-profile equalities are now theorem constructors under explicit mass-normalization of the observed carrier counts. `SinkhornGaugeThermodynamicsBridge` also records the source-backed finite fact that Sinkhorn normalization is diagonal Weyl-gauge transport whose RN force budget feeds KMS residual control. Remaining work is trajectory-level supply of mass certificates, not an ambient-norm bound.
4. D5 milestone: the finite `μ`/`log Z` owner lane is gate-tracked through `GrandCanonicalGaugePotentialBridge`, and the Fock-side `μN_B` coupling is gate-tracked through `GrandCanonicalFockNumberBridge`. This is a same-form bridge, not a theorem equating finite count profiles with Fock occupation readouts.
5. D6 milestone: `ChiralChargeFockNumberBridge` gate-tracks the source-owned chiral light-cone/KKT closure package: primitive `P₊ - P₋`, KKT/TKK `P_R - P_L`, Drazin supercharge `[P_D, Γ_G]`, right-minus-left anomaly closure, primitive CAR/CCR closure, and Fock occupation `a†a`. This is reconciliation, not a proof that all charges are identical.
6. D7 milestone: Formulate the fundamental hypotheses for the 4x4 Dirac-Souriau sector (Drazin decoupling, SUSY stability, entropy duality). Immediate next step is the constructive proof of the 4x4 Drazin inverse given an invertible topological block.
7. D8 milestone: The infinite Souriau Hessian interface now has a smooth Legendre discharge path. `SmoothLegendreInverseWitness` connects the abstract coadjoint entropy/inverse-Fisher readouts to `LegendreHessianInverseContext`, exposing the two-sided continuous-linear-map inverse laws. Remaining work is model construction and analytic derivation, not another abstract packet.
8. D9 milestone: The Souriau literature translator surface is now gate-tracked. `structuredSouriauKKTTranslatorPacket` combines the finite Massieu/Fisher/Fenchel/Onsager packet with an explicit Karush-Kuhn-Tucker stationarity shadow, preserving the distinction from chiral `KKTCore`. The same surface now exposes `claimW_representationWeight_lifts_to_densityTransport`, `claimS_superCoadjoint_stress_supercurrent_readouts`, `claimF_superSouriauFermionGas_packet`, `claimG_infiniteSuperCoadjointMetriplectic_packet`, `claimT_splitCl44_TKK_JordanLie_packet`, and `claimM_coadjointLeaf_Casimir_transverseOnsager_packet`, keeping weights, stress projections, super-fermion Fock/CAR/Weyl-supertrace claims, the infinite super-coadjoint metriplectic lift, TKK/Jordan-Lie closure, and leaf/transverse dissipation tied to owner contexts rather than prose. The downloaded/extracted source context is recorded under `artifacts/alexandria/souriau_lie_thermo_context/context_packet.md`; the super-fermion literature/background context is recorded under `artifacts/alexandria/super_souriau_fermion_context/`.
9. D10 milestone: `ConformalSquareFisherOnsagerPositiveContext` now gives a theorem-backed route from a conformal operatorial square-response witness to `ConformalFisherOnsagerPositiveContext.selfResponse_nonneg` and `fisherOnsagerProduction_nonneg`. This is dimension-agnostic on `EndH₂ := DoubledSpace H →L[ℝ] DoubledSpace H`; it does not use a finite response matrix or infer global positivity from the indefinite Krein carrier.
10. D11 milestone: `ConformalPositivePartitionWitness` and `ConformalOperatorAdmissibilityWitness` now give a theorem-backed route from explicit infinite operatorial partition data to the conformal KKT admissibility gate. This still does not prove global positivity from the Krein carrier; the next stronger target is a self-dual positive cone / symmetric-space constructor for the same witness.
