import proofs.SixStateGeneralizedPauliBasis
import Mathlib.Tactic.Ring

open SixStateGeneralizedPauliBasis

-- This file is auto-generated by a SymPy script to bypass tactic timeouts.

theorem colorWeyl_mul_explicit (c d c' d' : ZMod 3) :
    colorWeyl c d * colorWeyl c' d' = ((ω3 : ℂ) ^ ((c.val : ℕ) * (d'.val : ℕ))) • colorWeyl (c + c') (d + d') := by
  have hw2 : ω3^2 = -ω3 - 1 := by linear_combination omega3_root
  have hw3 : ω3^3 = 1 := by linear_combination (ω3 - 1) * omega3_root + 1
  have hw4 : ω3^4 = ω3 := by linear_combination (ω3^2 - ω3) * omega3_root + ω3
  match c.val, d.val, c'.val, d'.val with
  | 0, 0, 0, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 0, 0, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 0, 0, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 0, 1, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 0, 1, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 0, 1, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 0, 2, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 0, 2, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 0, 2, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 1, 0, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 1, 0, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 1, 0, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 1, 1, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 1, 1, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 1, 1, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 1, 2, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 1, 2, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 1, 2, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 2, 0, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 2, 0, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 2, 0, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 2, 1, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 2, 1, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 2, 1, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 2, 2, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 2, 2, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 0, 2, 2, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 0, 0, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 0, 0, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 0, 0, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 0, 1, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 0, 1, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 0, 1, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 0, 2, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 0, 2, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 0, 2, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 1, 0, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 1, 0, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 1, 0, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 1, 1, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 1, 1, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 1, 1, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 1, 2, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 1, 2, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 1, 2, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 2, 0, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 2, 0, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 2, 0, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 2, 1, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 2, 1, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 2, 1, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 2, 2, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 2, 2, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 1, 2, 2, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 0, 0, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 0, 0, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 0, 0, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 0, 1, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 0, 1, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 0, 1, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 0, 2, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 0, 2, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 0, 2, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 1, 0, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 1, 0, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 1, 0, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 1, 1, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 1, 1, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 1, 1, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 1, 2, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 1, 2, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 1, 2, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 2, 0, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 2, 0, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 2, 0, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 2, 1, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 2, 1, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 2, 1, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 2, 2, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 2, 2, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf
  | 2, 2, 2, 2 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [colorWeyl, colorShift, colorClock, Matrix.mul_apply, Fin.sum_univ_three, ZMod.val, Matrix.smul_apply, smul_eq_mul] <;>
    try simp only [hw2, hw3, hw4, pow_succ, pow_two, pow_one, pow_zero] <;>
    try ring_nf

theorem sheetWeyl_mul_explicit (a b c d : ZMod 2) :
    sheetWeyl a b * sheetWeyl c d = ((if (b.val : ℕ) * (c.val : ℕ) = 1 then (-1 : ℂ) else (1 : ℂ)) : ℂ) • sheetWeyl (a + c) (b + d) := by
  match a.val, b.val, c.val, d.val with
  | 0, 0, 0, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
  | 0, 0, 0, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
  | 0, 0, 1, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
  | 0, 0, 1, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
  | 0, 1, 0, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
  | 0, 1, 0, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
  | 0, 1, 1, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
  | 0, 1, 1, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
  | 1, 0, 0, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
  | 1, 0, 0, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
  | 1, 0, 1, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
  | 1, 0, 1, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
  | 1, 1, 0, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
  | 1, 1, 0, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
  | 1, 1, 1, 0 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
  | 1, 1, 1, 1 =>
    ext i j; fin_cases i <;> fin_cases j <;>
    simp only [sheetWeyl, sheetFlip, sheetGamma, sheetPlus, sheetMinus, Matrix.mul_apply, Fin.sum_univ_two, ZMod.val, Matrix.smul_apply, smul_eq_mul, pow_succ, pow_zero, pow_one, pow_two] <;>
    try norm_num
