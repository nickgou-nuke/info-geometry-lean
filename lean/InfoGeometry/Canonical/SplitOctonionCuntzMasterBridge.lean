import InfoGeometry.Algebra.TripotentClSUSYBridge
import InfoGeometry.Canonical.ColimitRigidityFixedLocusBridge

/-!
# Split-octonion/Cuntz bridge

The native tripotent Clifford laws are owned by
`TripotentClSUSYBridge`; the fixed-locus law is owned by
`ColimitRigidityFixedLocusBridge`.  This compatibility module intentionally
contains only those imports and no duplicate forwarding or master packet.
-/
