ring R = 0,x,dp;
int g2two = 12096;
int derived = 6048;
int pgl = 5616;
int splitCarrier = 256;
if (splitCarrier <> 2^8) { ERROR("split carrier cardinality failed"); }
if (g2two <> 2^6*(2^6-1)*(2^2-1)) { ERROR("G2(2) order formula failed"); }
if (derived*2 <> g2two) { ERROR("derived half-order failed"); }
if (pgl == g2two) { ERROR("PGL3(3) order separation failed"); }
print("SINGULAR_G2TWO_SPLIT_ZORN_OK");

