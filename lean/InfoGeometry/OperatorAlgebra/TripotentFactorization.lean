import InfoGeometry.OperatorAlgebra.GeneralizedNilpotentTripotent

/-!
# Retired matrix-only tripotent factorization

The former theorem in this file was a coordinate `Matrix ... ℝ` partition of
unity.  The canonical maintained owner is now
`GeneralizedNilpotentTripotent`, whose projectors and reconstruction theorem
are formulated on arbitrary module endomorphisms and proved in the
noncommutative endomorphism ring.

This file remains only as an import-compatible route for older umbrella files.
-/
