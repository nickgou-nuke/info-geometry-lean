import InfoGeometry.OperatorAlgebra.AndreevLedger
import InfoGeometry.OperatorAlgebra.VortexPunctureRepair

/-!
# Retired scalar vortex/confinement surface

The former file encoded a two-real-coordinate state and called the map
`a ↦ exp (-τ) a` a Radon--Nikodym transport.  That construction was neither a
measure transport nor an operatorial vortex index, and its finite quadratic
readouts did not establish spectral confinement.

The maintained vortex owners are the property-gated operator modules
`AndreevLedger` and `VortexPunctureRepair`.  They keep localization and
Majorana statements attached to explicit operator/core data.  This module is
kept only as a source-level retirement marker and exports no scalar surrogate
or critical-line theorem.
-/

namespace InfoGeometry.SuperMetriplectic.VortexConfinement

end InfoGeometry.SuperMetriplectic.VortexConfinement
