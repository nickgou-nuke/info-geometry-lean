import InfoGeometry.Clifford.NeutralPhaseSpaceCore
import InfoGeometry.Algebra.FiniteSpinAlgebra

/-!
# Compatibility import for the neutral dual-pair owner

The carrier, neutral forms, para-involution, and skew form are owned by
`InfoGeometry.Clifford.NeutralPhaseSpaceCore`.  This module intentionally
exports no aliases or duplicate theorem surface; it remains only so older
imports continue to resolve while clients migrate to the native names.
-/
