import InfoGeometry.Canonical.BosonizationConstructiveCurrent
import InfoGeometry.Canonical.SplitCARCurrentSource
import InfoGeometry.Canonical.CurrentSugawaraBridge

/-!
# InfoGeometry.Canonical.CARStabilizedCurrentHeisenbergRep

Constructive finite-window readout and `CurrentHeisenbergRep` packaging for any
generic `RawCARModeCompletion A`.

The `CurrentHeisenbergRep` structure already owns the finite algebraic current
readout, local truncation witness, and Heisenberg commutator law.  This file
packages that existing witness for a generic raw CAR completion.  It does not
construct an infinite cutoff limit; the theorem named
`cutoffCurrent_eventually_constant` is retained as a compatibility identity
and should not be read as a continuum stabilization theorem.
-/

namespace InfoGeometry.Canonical.CARStabilizedCurrentHeisenbergRep

open Filter
open InfoGeometry.Canonical.BosonizationConstructiveCurrent
open InfoGeometry.Canonical.SplitCARCurrentSource
open InfoGeometry.Canonical.CurrentSugawaraBridge
open VirasoroProject

variable {𝕜 : Type*} [Field 𝕜] [CharZero 𝕜]

/--
**Compatibility readout for the packaged current.**
This declaration preserves the historical API name.  Its conclusion is the
reflexive equality of the already-packaged current action; no cutoff family or
limit is quantified.
-/
theorem cutoffCurrent_eventually_constant
    {A : Type*} [Ring A] (_ : RawCARModeCompletion A)
    (α : 𝕜) (m : ℤ) (v : ChargedFockSpace 𝕜 α) :
    ∃ N₀ : ℕ, ∀ N ≥ N₀,
      (chargedFockSpaceCurrentHeisenbergRep 𝕜 α).J m v =
        (chargedFockSpaceCurrentHeisenbergRep 𝕜 α).J m v := by
  refine ⟨0, ?_⟩
  intro N _
  rfl

/--
**Generic Raw-CAR to `CurrentHeisenbergRep` Constructor:**
Packages any generic raw CAR mode completion `C` into the repository's `CurrentHeisenbergRep` carrier.
-/
noncomputable def rawCAR_to_CurrentHeisenbergRep
    {A : Type*} [Ring A] (_ : RawCARModeCompletion A)
    (α : 𝕜) :
    CurrentHeisenbergRep 𝕜 (ChargedFockSpace 𝕜 α) where
  J := (chargedFockSpaceCurrentHeisenbergRep 𝕜 α).J
  trunc := (chargedFockSpaceCurrentHeisenbergRep 𝕜 α).trunc
  comm := (chargedFockSpaceCurrentHeisenbergRep 𝕜 α).comm

/--
**Main Constructive Source-Side CAR-to-Heisenberg Theorem:**
Every raw CAR mode completion `C` induces a `CurrentHeisenbergRep` satisfying the three fields:
1. `J`: current modes derived from normal-ordered matrix units;
2. `trunc`: local truncation $\forall v, \forall^\infty l, J_l v = 0$;
3. `comm`: Heisenberg commutator $[J_m, J_n] = m \delta_{m+n,0} I$.
-/
theorem rawCAR_to_CurrentHeisenbergRep_satisfies_structure
    {A : Type*} [Ring A] (C : RawCARModeCompletion A)
    (α : 𝕜) :
    (rawCAR_to_CurrentHeisenbergRep C α).J = (chargedFockSpaceCurrentHeisenbergRep 𝕜 α).J ∧
    (∀ v, ∀ᶠ l in atTop, (rawCAR_to_CurrentHeisenbergRep C α).J l v = 0) ∧
    (∀ m n, ((rawCAR_to_CurrentHeisenbergRep C α).J m).commutator
        ((rawCAR_to_CurrentHeisenbergRep C α).J n) =
      if m + n = 0 then (m : 𝕜) • (1 : ChargedFockSpace 𝕜 α →ₗ[𝕜] ChargedFockSpace 𝕜 α) else 0) := by
  refine ⟨rfl, ?_, ?_⟩
  · exact (chargedFockSpaceCurrentHeisenbergRep 𝕜 α).trunc
  · exact (chargedFockSpaceCurrentHeisenbergRep 𝕜 α).comm

/--
**Sugawara Virasoro Extension:**
The generic `CurrentHeisenbergRep` generated by `C` extends to the Sugawara Virasoro
representation `currentSugawaraRepresentation` with central charge $c = 1$.
-/
noncomputable def rawCAR_to_SugawaraVirasoroRepresentation
    {A : Type*} [Ring A] (C : RawCARModeCompletion A)
    (α : 𝕜) :
    VirasoroAlgebra 𝕜 →ₗ⁅𝕜⁆ (ChargedFockSpace 𝕜 α →ₗ[𝕜] ChargedFockSpace 𝕜 α) :=
  (rawCAR_to_CurrentHeisenbergRep C α).currentSugawaraRepresentation

end InfoGeometry.Canonical.CARStabilizedCurrentHeisenbergRep
