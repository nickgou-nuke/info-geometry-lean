# Canonical Architecture Manifest

This manifest records the current stabilization layer of the `info-geometry-lean` repository. It separates the closed finite owner lemmas from later extensions and physical interpretations that remain explicit and conditional.

## The 3-Way Dictionary (The Core Finite Surface)

At the heart of the stabilized core is the **3-way operational dictionary**. This triad links combinatorial triangulation data, thermodynamic current algebra, and finite BCFW-style readouts. It should not be read as a global analytic equivalence theorem.

The equivalence rigorously binds:

1. **Klein-Null Triangulation** 
   * Represented by the $(2,2)$ split-twistor algebra over the projective boundary.
   * Defines the structural geometry of the Penrose spin network.
   * *Status:* Finite owner lemmas are present; Track B Macaulay ingestion is recorded as an explicit evidence boundary for `[X_3](3) = 1296`.

2. **Thermodynamic Flow**
   * Modeled as a causal non-equilibrium flow generated by the failure of forward and backward causal operators to commute ($P_{\text{forward}} P_{\text{backward}} - P_{\text{backward}} P_{\text{forward}} = \mathrm{d}\ln Q$).
   * Transforms the static boundary into a thermodynamic gauge network driven by entropy production.
   * *Status:* Finite ring-level flow algebra is formalized without local proof stubs (`Topology/ThermodynamicGauge.lean`).

3. **BCFW Residue Readout**
   * Records finite readouts inspired by on-shell residue bookkeeping.
   * Maps the thermodynamic potential to finite algebraic observables under explicit premises.
   * *Status:* Conditional finite readout surface; analytic scattering-amplitude claims remain outside the closed core (`Projective/OnShellResidueBCFWBridge.lean`).

4. **Finite q-Dilation / Pachner Readout**
   * Records the conservative super-Cuntz boundary packet, modular edge dilation, q-deficit readout, and Delaunay/Pachner transfer lemmas.
   * Serves as the finite bridge from q-supergrading to the thermodynamic and triangulation layers.
   * *Status:* Fully formalized as a finite interface without analytic C*-algebraic or continuum curvature claims (`Topology/SuperCuntzDilationCurvature.lean`).
   * *Colimit routing:* stagewise `n`-indexed data now routes through the inductive colimit owner (`Topology/SuperCuntzDilationColimit.lean`).

5. **Inductive-Colimit Transport**
   * Any theorem family indexed by `n` is not considered finished at the finite stage alone.
   * The final owner surface must provide an explicit inductive-colimit transport or a documented reason that the family is intentionally finite-stage only.
   * *Status:* Supported by the existing direct-limit and colimit lanes in `Categorical/`, `Canonical/`, and `Topological/`, but not automatic.

6. **Finite `Pin(5,5)` / Wallpaper Weyl Cross-Sections**
   * Records the exact finite root cross-section from the Klein-compatible wallpaper packet into the `D₅` Weyl shadow and the explicit affine coordinate bridge.
   * Contains only coordinate-level finite lemmas:
     `WallpaperPin55RootCrossSection.weylD5CrossSection_projects_wallpaper`,
     `WallpaperPin55RootCrossSection.weylD5CrossSection_action_projects_wallpaper`,
     `WallpaperAffineWeylD5Bridge.latticeEmbed_sigmaX`,
     `WallpaperAffineWeylD5Bridge.latticeEmbed_sigmaD`,
     `WallpaperAffineWeylD5Bridge.wallpaper_to_affine_weyl_d5_packet`.
   * *Status:* Closed finite owner layer. The repo still does **not** claim a full global classification of `O(5,5)`, `Pin(5,5)`, or the wallpaper groups.

7. **Finite Holographic Shard / Cantor KMS Split**
   * Records the exact dyadic shard identity and source reconstruction inside the Cantor/Cuntz boundary.
   * Contains only finite algebraic lemmas:
     `CantorKMSCylinderState.cylinderKMSWeight_children_sum`,
     `HolographicCuntzShard.finiteShard_reconstructs_source`,
     `HolographicCuntzShard.finite_holographic_shard_packet`.
   * *Status:* Closed finite owner layer. The repo still does **not** claim analytic Fourier holography, Reeh-Schlieder density, or a full GNS completion from this packet alone.

## Operational Law
This triad records a finite algebraic workflow: null-boundary data, thermodynamic-flow commutators, and BCFW-style readouts can be composed through explicit theorem premises. It does not prove a continuum spacetime-emergence theorem or an exact physical scattering-amplitude theorem.

Any future formalization extending this foundation—such as the non-abelian Wilson loop holonomy or the Bost-Connes partition function—must keep analytic and physical claims behind explicit premises and avoid proof-carrying certificate or witness packets.

The q-dilation corridor is a companion interface to the triad above, not a replacement for it: it preserves the same finite-owner discipline while exposing the edge-scaling and deficit readouts used by the Delaunay/Pachner layer.
Where the q-corridor is indexed by `n`, the code now routes it through the inductive colimit layer rather than treating the finite packet as the terminal object.

For the next lane, see [FIBONACCI_MZM_FOLLOWUP_ROADMAP.md](FIBONACCI_MZM_FOLLOWUP_ROADMAP.md). It is the intended handoff for braid/Majorana comparisons and remains explicitly assumption-gated.

The same note applies to every `n`-dependent theorem family in the repo: finite-stage theorems are useful, but they are not the terminus unless they are transported to an inductive colimit owner.
