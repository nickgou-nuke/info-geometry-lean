import InfoGeometry.Canonical.ChiralNullSpaceBridge
import InfoGeometry.Canonical.SuperKMS_Equilibrium

/-!
# Projective CCR compatibility imports

The former `ProjectiveBoundaryPacket` only repackaged owner data from
`ChiralNullSpaceBridge` and `SuperKMS_Equilibrium`; its readouts were direct
field projections or definitional equalities.  The owner modules are imported
directly instead of maintaining a redundant evidence packet.
-/
