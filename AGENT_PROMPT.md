# Agentic Navigation & Epistemic Operating System

Welcome, Agent. You have been summoned to explore, verify, and expand **InfoGeometry: The Spinorial Prima Materia**.

This repository is an executable mathematical organism. Treat it with epistemic precision.

---

### Operating Laws

1. **Build Cache Inviolability:**
   * **NEVER RUN `lake clean`** or delete `.lake/build`.
   * Preserving the compiled `.olean` cache across 23,000+ targets is essential.
   * Sequential compilation only: never invoke concurrent builds.

2. **The Colimit Continuum Law:**
   * The continuum is reached strictly through categorical direct inductive colimits (`UHFInductiveColimitBoundary.lean`, `TensorTowerColimit.lean`).
   * No analytical hallucinations or fake measure-theoretic shortcuts.

3. **Truth Boundary:**
   * Graph databases and computer algebra systems identify candidate wires.
   * Lean 4 owner files decide truth.
   * Only kernel-checked Lean source edits count.
   * Zero `sorry`, zero `admit`, zero wrapper theorems, zero proxy witnesses.

4. **Continuous Git Tracking:**
   * Always run `git add -A` after creating or modifying files to protect against destructive rollbacks.

5. **The Synthesis Invariants:**
   * **BCFW Recursion** = Mixed Arnold-Cohen relations.
   * **On-Shell Factorization** = The Klein quadric boundary ( = 0$) represented by nilpotent chiral Cuntz generators (\pm^2 = 0$).
   * **Amplituhedron Volume / Integrand** = Bost-Connes KMS state partition function.

---

### Suggested Queries to Run First

1. Trace the root import:
   `lake build InfoGeometry.All`
2. Query the declaration graph:
   Use the ArangoDB declaration service on port `8530` (`infogeometry` database) to inspect dependency centrality, roots, and topological voids.
3. Check open frontiers:
   Locate leaf modules in `InfoGeometry.Canonical.*` and close intermediate lemmas by building upon established algebraic structures in `InfoGeometry.Algebra.*` and `InfoGeometry.Quantum.*`.
