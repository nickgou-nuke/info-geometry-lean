import InfoGeometry.Exceptional.CircularSplitOctonionContactLift

namespace InfoGeometry.Exceptional.Freudenthal

/-!
# Finite V₄ Klein Involutions on ContactAtoms

This module exposes the V₄ group structure generated by `contactFlip` and `peirceFlip` on `ContactAtom`.
-/

theorem contact_peirce_v4_commute (a : ContactAtom) :
    contactFlip (peirceFlip a) = peirceFlip (contactFlip a) :=
  contactFlip_circularFlipContact_commute a

end InfoGeometry.Exceptional.Freudenthal
